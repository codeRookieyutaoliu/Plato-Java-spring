# 更新日志

本文件记录项目所有版本的重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased] - 开发中

### 新增
- 添加Git与CI/CD规范
- 添加代码风格检查配置
- 添加Docker构建配置
- 添加GitHub Actions工作流程

## [0.1.0] - 项目初始化

### 新增
- 项目基础架构搭建
- 添加plato-common模块
- 添加plato-client模块
- 添加plato-gateway模块
- 添加plato-ipconf模块
- 添加plato-state模块 


### 重构common模块

```
plato-framework/
│
├── plato-common/                   # 通用基础设施
│   ├── src/main/java/plato/common/
│   │   ├── exception/              # 异常定义
│   │   ├── utils/                  # 工具类
│   │   ├── constants/              # 常量定义
│   │   └── model/                  # 基础模型
│   └── pom.xml
│
├── plato-message/                  # 消息领域
│   ├── src/main/java/plato/message/
│   │   ├── domain/                 # 消息领域模型
│   │   │   ├── entity/             # 消息实体
│   │   │   ├── valueobject/        # 消息值对象
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   │   ├── service/            # 应用服务
│   │   │   └── dto/                # 数据传输对象
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── repository/         # 仓储实现
│   │   │   ├── codec/              # 编解码实现
│   │   │   └── proto/              # Protobuf定义
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-network/                  # 网络领域
│   ├── src/main/java/plato/network/
│   │   ├── domain/                 # 网络领域模型
│   │   │   ├── entity/             # 连接实体
│   │   │   ├── valueobject/        # 网络值对象
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── netty/              # Netty实现
│   │   │   └── tcp/                # TCP处理
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-cache/                    # 缓存领域
│   ├── src/main/java/plato/cache/
│   │   ├── domain/                 # 缓存领域模型
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── redis/              # Redis实现
│   │   │   └── local/              # 本地缓存实现
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-discovery/                # 服务发现领域
│   ├── src/main/java/plato/discovery/
│   │   ├── domain/                 # 领域模型
│   │   │   ├── entity/             # 服务实体
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   └── nacos/              # Nacos实现
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-config/                   # 配置领域
│   ├── src/main/java/plato/config/
│   │   ├── domain/                 # 领域模型
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   └── yaml/               # YAML配置支持
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-bus/                      # 事件总线领域
│   ├── src/main/java/plato/bus/
│   │   ├── domain/                 # 领域模型
│   │   │   ├── event/              # 事件定义
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-bizflow/                  # 业务流程领域
│   ├── src/main/java/plato/bizflow/
│   │   ├── domain/                 # 领域模型
│   │   │   ├── entity/             # 流程实体
│   │   │   ├── valueobject/        # 流程值对象
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   │   ├── service/            # 引擎服务
│   │   │   └── dto/                # 数据传输对象
│   │   ├── infrastructure/         # 基础设施层
│   │   │   └── graph/              # DAG图实现
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-router/                   # 路由领域
│   ├── src/main/java/plato/router/
│   │   ├── domain/                 # 领域模型
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-rpc/                      # RPC领域
│   ├── src/main/java/plato/rpc/
│   │   ├── domain/                 # 领域模型
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── server/             # 服务端实现
│   │   │   ├── client/             # 客户端实现
│   │   │   ├── interceptor/        # 拦截器
│   │   │   └── lb/                 # 负载均衡
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-timingwheel/              # 时间轮领域
│   ├── src/main/java/plato/timingwheel/
│   │   ├── domain/                 # 领域模型
│   │   │   ├── entity/             # 时间轮实体
│   │   │   ├── valueobject/        # 值对象
│   │   │   └── service/            # 领域服务
│   │   ├── application/            # 应用层
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── wheel/              # 时间轮实现
│   │   │   ├── bucket/             # 任务桶实现
│   │   │   └── queue/              # 延迟队列实现
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
├── plato-logger/                   # 日志领域
│   ├── src/main/java/plato/logger/
│   │   ├── domain/                 # 领域模型
│   │   ├── application/            # 应用层
│   │   │   └── service/            # 日志服务
│   │   ├── infrastructure/         # 基础设施层
│   │   │   ├── slf4j/              # SLF4J实现
│   │   │   └── trace/              # 链路跟踪实现
│   │   └── interfaces/             # 接口层
│   └── pom.xml
│
└── pom.xml                         # 父pom配置

```

## 下一步开发计划

根据当前的项目状态，建议按照以下步骤继续进行开发：

### 完善 plato-message 模块
- 实现消息的应用层服务，提供消息处理和转换功能
- 完善消息的基础设施层，包括消息编解码、序列化和反序列化
- 添加单元测试，确保消息处理的正确性

### 开发 plato-network 模块
- 根据项目架构设计，实现网络连接和通信的核心功能
- 基于 Netty 框架实现高性能的网络通信
- 设计连接管理、消息发送和接收的机制

### 重构 plato-gateway 模块
- 按照设计文档，实现网关的核心功能
- 集成 plato-message 和 plato-network 模块
- 实现连接管理、消息路由和处理逻辑

### 开发 plato-state 模块
- 实现状态管理的核心功能
- 设计与网关模块的交互机制

以上开发计划应遵循项目的 Git 工作流规范，使用 feature 分支进行开发，完成后合并到 develop 分支。每个步骤完成后应添加充分的单元测试和文档。

需要特别注意的是，所有的实现都应保持与 Go 项目的兼容性，尤其是消息结构和通信协议方面。

## 开发进度记录

### 2023年4月2日
- 完成了项目知识库搭建，添加了模块职责分析和消息处理架构说明
- 实现了 plato-common 模块的基础功能，包括：
  - 异常体系（PlatoException及其子类）
  - 基础工具类（HashUtils）
  - 系统常量定义（SystemConstants）
  - 通用返回结果模型（Result）
- 实现了 plato-message 模块的基本功能，包括：
  - Protocol Buffers 消息定义（message.proto）
  - 消息领域实体（Message接口及实现类）
  - 消息编解码器（MessageCodec）
  - 消息转换器（MessageConverter）
  - 消息处理器架构（MessageHandler接口、AbstractMessageHandler）
  - 基本消息处理器实现（LoginMessageHandler、HeartbeatMessageHandler）
- 项目代码已纳入 Git 版本控制，当前在 `feature/framework-refactor-common` 分支开发

### 2023年4月3日
- 完成了plato-message模块的全部消息处理器实现，包括：
  - 登录消息处理器（LoginMessageHandler）
  - 心跳消息处理器（HeartbeatMessageHandler）
  - 重连消息处理器（ReconnectMessageHandler）
  - 上行消息处理器（UpMessageHandler）
  - 确认消息处理器（AckMessageHandler）
  - 推送消息处理器（PushMessageHandler）
- 每个处理器都包含详细的日志记录和异常处理机制
- 所有处理器都遵循统一的处理模式，继承自AbstractMessageHandler
- 完成了消息工厂（MessageFactory）的完整实现，增加了以下功能：
  - 消息ID生成器，确保每个消息都有唯一ID
  - 消息类型缓存，提高消息类型查找效率
  - 消息验证机制，确保消息符合基本格式要求
  - 完善的日志记录和异常处理
  - 丰富的工厂方法，支持创建各种类型的消息
  - 成功/失败消息确认的便捷方法

## plato-message模块分析与完善建议

### 职责边界说明

在进一步分析前，有必要明确 plato-message 模块与 plato-network 模块之间的职责边界：

**plato-message 模块职责**：
- 定义消息结构和类型（通过Protocol Buffers）
- 提供消息的领域模型实现
- 实现消息的序列化和反序列化
- 提供消息与领域对象之间的转换
- 定义消息处理接口和抽象实现
- 提供基本的消息处理业务逻辑

**plato-network 模块职责**：
- 处理底层网络通信（基于Netty）
- 管理网络连接的建立和断开
- 处理消息的编码和解码（网络层面）
- 实现网络层面的消息分发
- 管理网络会话和连接状态
- 处理网络异常和重连机制

在之前的分析中，有些职责划分不够清晰，部分network模块的职责被归入message模块。下面的分析将更准确地反映各模块的实际职责。

### 已实现功能

通过对Go项目和Java项目代码的对比分析，目前plato-message模块已实现以下功能：

1. **消息定义**：
   - 使用Protocol Buffers完整定义了所有消息类型（CmdType枚举和各类消息结构）
   - 消息结构与Go项目保持一致，包括MsgCmd、UpMsg、PushMsg、ACKMsg等
   - 使用枚举MessageType替代了Go中的CmdType，保持了语义一致性

2. **领域模型**：
   - 实现了Message基础接口和AbstractMessage抽象类
   - 针对各类消息实现了具体的领域实体类（LoginMessage、HeartbeatMessage等）
   - 这些领域实体类与Protocol Buffers自动生成的类是分开的，领域实体类负责业务逻辑，而Protocol Buffers生成的类负责序列化/反序列化
   - 领域模型设计符合DDD原则，将业务逻辑与消息传输分离

3. **消息编解码**：
   - 实现了MessageCodec类，负责消息的业务层面编解码
   - 实现了MessageConverter，负责Protocol Buffers生成的对象与自定义领域对象的转换

4. **消息工厂**：
   - 实现了MessageFactory，用于创建各种类型的消息
   - 支持从Protocol Buffers字节流创建领域对象和反向转换
   - 负责协调Protocol Buffers生成的类与领域模型类之间的转换

5. **消息处理架构**：
   - 定义了MessageHandler接口，对应Go项目中的消息处理函数
   - 实现了AbstractMessageHandler抽象类，提供公共处理逻辑
   - 实现了部分具体消息处理器（LoginMessageHandler、HeartbeatMessageHandler）

### 待完善功能

与Go项目相比，plato-message模块还需要完善以下功能：

1. **消息处理器全量实现**：
   - 需要实现ReconnectMessageHandler（重连消息处理）
   - 需要实现UpMessageHandler（上行消息处理）
   - 需要实现AckMessageHandler（确认消息处理）
   - 需要实现PushMessageHandler（推送消息处理）

2. **消息处理的业务逻辑**：
   - 实现消息处理器中的具体业务逻辑
   - 处理消息的验证和业务规则校验
   - 与其他模块的交互逻辑

3. **消息可靠性机制（业务层面）**：
   - 实现上行消息可靠性保证（client_id比较和递增）
   - 实现下行消息可靠性保证（业务层面的消息确认机制）
   - 在领域层实现可靠性逻辑，而不是依赖Protobuf生成的类

4. **消息转换的完整实现**：
   - 完善MessageConverter，确保所有类型的Protobuf消息都能正确转换为领域对象
   - 实现高效的双向转换，最小化序列化/反序列化开销
   - 处理转换过程中可能出现的异常情况

5. **单元测试**：
   - 为所有消息类型和处理器添加单元测试
   - 测试消息编解码和转换的正确性
   - 测试消息处理的业务逻辑
   - 针对Protobuf生成类和领域模型类的交互进行测试

### 与plato-network模块的集成点

plato-message模块需要与plato-network模块进行集成，主要集成点包括：

1. **消息编解码集成**：
   - plato-message提供消息的业务层编解码逻辑
   - plato-network负责使用这些编解码器进行网络层的消息处理

2. **消息处理调用链**：
   - plato-network接收网络消息后，调用plato-message的处理器进行业务处理
   - plato-message处理完成后，通过plato-network发送响应消息

3. **消息工厂使用**：
   - plato-network在需要创建或解析消息时，调用plato-message的消息工厂

4. **事件通知机制**：
   - plato-message处理完消息后，通过事件机制通知plato-network相关状态变更
   - plato-network根据这些事件执行网络层面的操作（如断开连接、发送心跳等）

### 改进建议

1. **完善事件机制**：
   - 引入领域事件，使消息处理结果能够以事件形式通知其他组件
   - 通过事件总线实现模块间的松耦合通信

2. **增强消息追踪能力**：
   - 添加消息处理的日志和监控点
   - 记录消息处理的各个阶段和状态变更

3. **优化性能**：
   - 使用对象池减少消息对象创建和垃圾回收
   - 优化序列化和反序列化过程

4. **增强错误处理**：
   - 更详细的错误分类和处理机制
   - 在消息处理过程中增加异常捕获和恢复机制

### 下一步工作重点

1. 完成所有消息处理器的业务逻辑实现
2. 实现消息与其他模块的集成点
3. 完善消息转换机制
4. 添加单元测试和集成测试

以上分析和建议基于对Go项目源码和Java项目当前实现的对比，明确了plato-message模块的职责边界，确保与plato-network模块有清晰的分工。

## 当前进度TODO列表

### plato-framework 模块重构

#### plato-common 模块
- [x] 基础项目结构搭建
- [x] Maven依赖配置
- [x] 异常定义与处理（PlatoException, BusinessException, SystemException, NetworkException）
- [x] 基础工具类实现（HashUtils）
- [x] 常量定义（SystemConstants）
- [x] 通用模型定义（Result）
- [ ] 更多工具类实现（日期、字符串、集合等）
- [ ] 线程池和并发工具实现
- [ ] 网络工具类实现
- [ ] 配置加载和管理工具
- [ ] 单元测试

#### plato-message 模块
- [x] 基础项目结构搭建
- [x] Maven依赖配置
- [x] Protocol Buffers消息定义
- [x] 消息DTO定义
- [x] 基础消息处理器接口设计
- [x] 消息领域实体建模（Message接口及各消息类型实现）
- [x] 消息编解码实现（MessageCodec）
- [x] 消息转换器实现（MessageConverter）
- [x] 抽象消息处理器实现（AbstractMessageHandler）
- [x] 登录消息处理器实现（LoginMessageHandler）
- [x] 心跳消息处理器实现（HeartbeatMessageHandler）
- [x] 重连消息处理器实现（ReconnectMessageHandler）
- [x] 上行消息处理器实现（UpMessageHandler）
- [x] 确认消息处理器实现（AckMessageHandler）
- [x] 推送消息处理器实现（PushMessageHandler）
- [x] 消息工厂完整实现
- [ ] 单元测试

#### plato-network 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] Netty集成
- [ ] 连接管理实现
- [ ] 消息发送接收机制
- [ ] 网络异常处理
- [ ] 单元测试

#### plato-cache 模块
- [ ] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 本地缓存实现
- [ ] Redis集成
- [ ] 缓存策略定义
- [ ] 单元测试

#### plato-discovery 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 服务发现接口定义
- [ ] Nacos集成
- [ ] 单元测试

#### plato-config 模块
- [ ] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 配置加载机制
- [ ] YAML配置支持
- [ ] 动态配置更新
- [ ] 单元测试

#### plato-bizflow 模块
- [ ] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] DAG引擎设计
- [ ] 流程执行机制
- [ ] 单元测试

#### plato-timingwheel 模块
- [ ] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 时间轮算法实现
- [ ] 定时任务管理
- [ ] 单元测试

### 业务模块实现

#### plato-gateway 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 长连接管理
- [ ] 消息路由
- [ ] 与state模块交互
- [ ] 消息转发
- [ ] 单元测试

#### plato-state 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 连接状态管理
- [ ] 心跳处理
- [ ] 重连机制
- [ ] 单元测试

#### plato-ipconf 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] IP列表管理
- [ ] 动态调度算法
- [ ] HTTP接口
- [ ] 单元测试

#### plato-client 模块
- [x] 基础项目结构搭建
- [ ] Maven依赖配置
- [ ] 命令行解析
- [ ] SDK实现
- [ ] 用户界面
- [ ] 集成测试

### 跨功能任务

- [ ] 完善项目文档
- [ ] 代码风格统一
- [ ] CI/CD流程完善
- [ ] 性能测试
- [ ] 安全测试

以上TODO列表将随着项目进展持续更新，已完成的任务会被标记为[x]，待完成的任务标记为[ ]。
