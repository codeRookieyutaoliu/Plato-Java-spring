# 更新日志

本文件记录项目所有版本的重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。



## common 模块重构为 framework(未完成，暂时中断)









开发进度记录

### 2023 年 4 月 2 日
- 完成了项目知识库搭建，添加了模块职责分析和消息处理架构说明
- 实现了 plato-common 模块的基础功能，包括：
    - 异常体系（PlatoException 及其子类）
    - 基础工具类（HashUtils）
    - 系统常量定义（SystemConstants）
    - 通用返回结果模型（Result）
- 实现了 plato-message 模块的基本功能，包括：
    - Protocol Buffers 消息定义（message.proto）
    - 消息领域实体（Message 接口及实现类）
    - 消息编解码器（MessageCodec）
    - 消息转换器（MessageConverter）
    - 消息处理器架构（MessageHandler 接口、AbstractMessageHandler）
    - 基本消息处理器实现（LoginMessageHandler、HeartbeatMessageHandler）
- 项目代码已纳入 Git 版本控制，当前在 `feature/framework-refactor-common` 分支开发

### 2023 年 4 月 3 日
- 完成了 plato-message 模块的全部消息处理器实现，包括：
    - 登录消息处理器（LoginMessageHandler）
    - 心跳消息处理器（HeartbeatMessageHandler）
    - 重连消息处理器（ReconnectMessageHandler）
    - 上行消息处理器（UpMessageHandler）
    - 确认消息处理器（AckMessageHandler）
    - 推送消息处理器（PushMessageHandler）
- 每个处理器都包含详细的日志记录和异常处理机制
- 所有处理器都遵循统一的处理模式，继承自 AbstractMessageHandler
- 完成了消息工厂（MessageFactory）的完整实现，增加了以下功能：
    - 消息 ID 生成器，确保每个消息都有唯一 ID
    - 消息类型缓存，提高消息类型查找效率
    - 消息验证机制，确保消息符合基本格式要求
    - 完善的日志记录和异常处理
    - 丰富的工厂方法，支持创建各种类型的消息
    - 成功/失败消息确认的便捷方法
- 完成了 plato-network 模块的核心功能实现，包括：
    - 创建了 Connection 连接实体，用于表示网络连接
    - 设计了 ConnectionManager 接口，定义连接管理的核心功能
    - 实现了 DefaultConnectionManager，基于 ConcurrentHashMap 的高并发连接管理
    - 实现了消息编解码器（MessageDecoder 和 MessageEncoder），处理网络层的消息序列化
    - 实现了消息通道处理器（MessageChannelHandler），处理连接建立、断开和消息接收
    - 实现了 NettyServer 服务器类，配置并启动 Netty 服务器
    - 实现了 NettyClient 客户端类，用于连接服务器
    - 所有实现都包含完整的日志记录和异常处理机制
    - 实现了心跳和重连机制

### 2023 年 4 月 10 日
- 增强了 plato-network 模块的功能，新增或完善以下功能：
    - 添加了 ConnectionStatistics 值对象，用于收集和统计连接及消息相关指标
    - 实现了 ConnectionMonitorService 服务，用于定期检查和清理空闲连接，收集连接统计信息
    - 更新了 DefaultConnectionManager，集成了连接统计功能
    - 更新了 MessageChannelHandler，支持消息统计功能
    - 更新了 NettyServer 和 NettyClient，集成了连接监控服务
    - 实现了 ConnectionPool 连接池类，用于管理多个客户端连接
    - 实现了 ConnectionPoolManager 连接池管理器，管理多个连接池
    - 所有新增功能都有详细的日志记录和完善的异常处理机制
    - 性能优化：使用原子类实现线程安全的计数器，优化了连接管理和监控的性能
    - 可观测性增强：添加了详细的统计信息和状态监控功能

#### plato-message 模块分析与完善建议

##### 职责边界说明

在进一步分析前，有必要明确 plato-message 模块与 plato-network 模块之间的职责边界：

**plato-message 模块职责**：

- 定义消息结构和类型（通过 Protocol Buffers）
- 提供消息的领域模型实现
- 实现消息的序列化和反序列化
- 提供消息与领域对象之间的转换
- 定义消息处理接口和抽象实现
- 提供基本的消息处理业务逻辑

**plato-network 模块职责**：
- 处理底层网络通信（基于 Netty）
- 管理网络连接的建立和断开
- 处理消息的编码和解码（网络层面）
- 实现网络层面的消息分发
- 管理网络会话和连接状态
- 处理网络异常和重连机制

在之前的分析中，有些职责划分不够清晰，部分 network 模块的职责被归入 message 模块。下面的分析将更准确地反映各模块的实际职责。

##### 已实现功能

通过对 Go 项目和 Java 项目代码的对比分析，目前 plato-message 模块已实现以下功能：

1. **消息定义**：
    - 使用 Protocol Buffers 完整定义了所有消息类型（CmdType 枚举和各类消息结构）
    - 消息结构与 Go 项目保持一致，包括 MsgCmd、UpMsg、PushMsg、ACKMsg 等
    - 使用枚举 MessageType 替代了 Go 中的 CmdType，保持了语义一致性

2. **领域模型**：
    - 实现了 Message 基础接口和 AbstractMessage 抽象类
    - 针对各类消息实现了具体的领域实体类（LoginMessage、HeartbeatMessage 等）
    - 这些领域实体类与 Protocol Buffers 自动生成的类是分开的，领域实体类负责业务逻辑，而 Protocol Buffers 生成的类负责序列化/反序列化
    - 领域模型设计符合 DDD 原则，将业务逻辑与消息传输分离

3. **消息编解码**：
    - 实现了 MessageCodec 类，负责消息的业务层面编解码
    - 实现了 MessageConverter，负责 Protocol Buffers 生成的对象与自定义领域对象的转换

4. **消息工厂**：
    - 实现了 MessageFactory，用于创建各种类型的消息
    - 支持从 Protocol Buffers 字节流创建领域对象和反向转换
    - 负责协调 Protocol Buffers 生成的类与领域模型类之间的转换

5. **消息处理架构**：
    - 定义了 MessageHandler 接口，对应 Go 项目中的消息处理函数
    - 实现了 AbstractMessageHandler 抽象类，提供公共处理逻辑
    - 实现了部分具体消息处理器（LoginMessageHandler、HeartbeatMessageHandler）

##### 待完善功能

与 Go 项目相比，plato-message 模块还需要完善以下功能：

1. **消息处理器全量实现**：
    - 需要实现 ReconnectMessageHandler（重连消息处理）
    - 需要实现 UpMessageHandler（上行消息处理）
    - 需要实现 AckMessageHandler（确认消息处理）
    - 需要实现 PushMessageHandler（推送消息处理）

2. **消息处理的业务逻辑**：
    - 实现消息处理器中的具体业务逻辑
    - 处理消息的验证和业务规则校验
    - 与其他模块的交互逻辑

3. **消息可靠性机制（业务层面）**：
    - 实现上行消息可靠性保证（client_id 比较和递增）
    - 实现下行消息可靠性保证（业务层面的消息确认机制）
    - 在领域层实现可靠性逻辑，而不是依赖 Protobuf 生成的类

4. **消息转换的完整实现**：
    - 完善 MessageConverter，确保所有类型的 Protobuf 消息都能正确转换为领域对象
    - 实现高效的双向转换，最小化序列化/反序列化开销
    - 处理转换过程中可能出现的异常情况

5. **单元测试**：
    - 为所有消息类型和处理器添加单元测试
    - 测试消息编解码和转换的正确性
    - 测试消息处理的业务逻辑
    - 针对 Protobuf 生成类和领域模型类的交互进行测试

### 与 plato-network 模块的集成点

plato-message 模块需要与 plato-network 模块进行集成，主要集成点包括：

1. **消息编解码集成**：
    - plato-message 提供消息的业务层编解码逻辑
    - plato-network 负责使用这些编解码器进行网络层的消息处理

2. **消息处理调用链**：
    - plato-network 接收网络消息后，调用 plato-message 的处理器进行业务处理
    - plato-message 处理完成后，通过 plato-network 发送响应消息

3. **消息工厂使用**：
    - plato-network 在需要创建或解析消息时，调用 plato-message 的消息工厂

4. **事件通知机制**：
    - plato-message 处理完消息后，通过事件机制通知 plato-network 相关状态变更
    - plato-network 根据这些事件执行网络层面的操作（如断开连接、发送心跳等）

### 改进建议

1. **完善事件机制**：
    - 引入领域事件，使消息处理结果能够以事件形式通知其他组件
    - 通过事件总线实现模块间的松耦合通信

2. **增强消息追踪能力**：
    - 添加消息处理的日志和监控点
    - 记录消息处理的各个阶段和状态变更

3. **优化性能**：
    - 使用对象池减少消息对象创建和垃圾回收
    - 优化序列化和反序列化过程

4. **增强错误处理**：
    - 更详细的错误分类和处理机制
    - 在消息处理过程中增加异常捕获和恢复机制

### 下一步工作重点

1. 完成所有消息处理器的业务逻辑实现
2. 实现消息与其他模块的集成点
3. 完善消息转换机制
4. 添加单元测试和集成测试

以上分析和建议基于对 Go 项目源码和 Java 项目当前实现的对比，明确了 plato-message 模块的职责边界，确保与 plato-network 模块有清晰的分工。

### 当前进度 TODO 列表

### plato-framework 模块重构

#### plato-common 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] 异常定义与处理（PlatoException, BusinessException, SystemException, NetworkException）
- [x] 基础工具类实现（HashUtils）
- [x] 常量定义（SystemConstants）
- [x] 通用模型定义（Result）
- [ ] 更多工具类实现（日期、字符串、集合等）
- [ ] 线程池和并发工具实现
- [ ] 网络工具类实现
- [ ] 配置加载和管理工具
- [ ] 单元测试

#### plato-message 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] Protocol Buffers 消息定义
- [x] 消息 DTO 定义
- [x] 基础消息处理器接口设计
- [x] 消息领域实体建模（Message 接口及各消息类型实现）
- [x] 消息编解码实现（MessageCodec）
- [x] 消息转换器实现（MessageConverter）
- [x] 抽象消息处理器实现（AbstractMessageHandler）
- [x] 登录消息处理器实现（LoginMessageHandler）
- [x] 心跳消息处理器实现（HeartbeatMessageHandler）
- [x] 重连消息处理器实现（ReconnectMessageHandler）
- [x] 上行消息处理器实现（UpMessageHandler）
- [x] 确认消息处理器实现（AckMessageHandler）
- [x] 推送消息处理器实现（PushMessageHandler）
- [x] 消息工厂完整实现
- [ ] 单元测试

#### plato-network 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] Netty 集成
- [x] 连接实体设计与实现
- [x] 连接管理器接口设计
- [x] 连接管理器实现
- [x] 消息编解码器实现
- [x] 消息通道处理器实现
- [x] Netty 服务器实现
- [x] Netty 客户端实现
- [x] 连接统计和监控功能（ConnectionStatistics）
- [x] 连接监控服务（ConnectionMonitorService）
- [x] 连接池实现（ConnectionPool）
- [x] 连接池管理器实现（ConnectionPoolManager）
- [ ] 单元测试

#### plato-cache 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] 本地缓存实现
- [x] Redis 集成
- [x] 缓存策略定义
- [x] 单元测试

#### plato-cache 模块详细进度

1. **核心功能**
    - [x] 设计并实现 Cache 接口(mSet/mGet/mDel 方法)
    - [x] 实现 CacheMode 枚举(LOCAL/REMOTE)
    - [x] 实现 CacheOptions 配置类
    - [x] 设计并实现 CacheManager 接口
    - [x] 实现 DefaultCacheManager(多级缓存管理)
    - [x] 实现 CacheService 便捷操作类
    - [x] 实现 CacheConstants 常量类

2. **本地缓存**
    - [x] 基于 Caffeine 实现高性能 LocalCache
    - [x] 支持最大缓存条目配置
    - [x] 支持缓存过期时间配置
    - [x] 实现批量读写删除操作

3. **Redis 缓存**
    - [x] 基于 Spring Data Redis 实现 RedisCache
    - [x] 支持键前缀配置
    - [x] 支持 Lua 脚本执行
    - [x] 实现序列化和反序列化处理
    - [x] 实现各种数据类型操作(String/Hash/List)

4. **高级特性**
    - [x] 多级缓存策略(本地缓存和 Redis 缓存协同)
    - [x] 缓存回写机制(Redis 到本地缓存)
    - [x] 批量操作优化
    - [x] 自动装配(Spring Boot 集成)

5. **测试与文档**
    - [x] 本地缓存单元测试
    - [x] Redis 缓存单元测试
    - [x] 缓存管理器单元测试
    - [x] 缓存服务单元测试
    - [x] 编写模块 README 文档

6. **待实现功能**
    - [ ] Redis Set 类型操作支持(SADD/SREM)
    - [ ] 分布式锁功能
    - [ ] 缓存监控和统计功能
    - [ ] 缓存一致性机制

#### plato-discovery 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] 服务发现接口定义
- [x] Nacos 集成
- [ ] 单元测试

#### plato-config 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] 配置模型定义（ConfigProperty, ConfigGroup）
- [x] 配置服务接口设计（ConfigService）
- [x] 配置变更监听机制（ConfigChangeListener）
- [x] 本地配置服务实现（LocalConfigService）
- [x] Nacos 配置服务实现（NacosConfigService）
- [x] REST API 接口实现（ConfigController）
- [x] 自动配置支持（PlatoConfigAutoConfiguration）
- [x] 示例配置文件（plato-config-example.yaml, plato-system.yaml）
- [x] 模块文档完善（README.md）
- [ ] 单元测试

#### plato-bizflow 模块
- [x] 基础项目结构搭建
- [x] Maven 依赖配置
- [x] 核心模型定义（Node 接口、AbstractNode 抽象类、RetryStrategy）
- [x] 流程执行上下文（ProcessContext）和执行结果（NodeResult）定义
- [x] 执行状态枚举（ExecutionStatus）
- [x] 节点执行器接口设计（NodeExecutor 和 ExecuteCallback）
- [x] 节点执行器实现（DefaultNodeExecutor）
    - 支持异步节点执行
    - 实现超时控制机制
    - 实现错误处理和重试机制
    - 支持执行状态跟踪
    - 提供执行前后回调
- [x] 图模型实现（Graph 类、Edge 类）
- [x] DAG 构建实现（GraphBuilder、循环检测）
- [x] 拓扑排序实现（TopologicalSorter）
- [x] 循环依赖检测和异常处理（CyclicDependencyException）
- [x] 图执行器接口设计（GraphExecutor）
- [x] 图执行器实现（DefaultGraphExecutor）
    - 支持异步、同步执行整个图
    - 支持执行特定路径的节点
    - 支持从特定节点开始执行
    - 支持取消和状态跟踪
    - 实现节点依赖管理
- [ ] 应用服务实现（ProcessService、ProcessExecutionService）
- [ ] 与事件总线集成
- [ ] REST API 接口实现（ProcessController）
- [ ] 可视化数据支持
- [ ] 执行历史和指标收集
- [ ] 单元测试

#### plato-timingwheel 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 时间轮算法实现
- [ ] 定时任务管理
- [ ] 单元测试

#### plato-bus 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 事件模型定义（Event 接口、EventHeader、EventType）
- [ ] 事件总线接口设计（EventBus）
- [ ] 事件发布者和订阅者接口（EventPublisher、EventSubscriber）
- [ ] 本地事件总线实现（LocalEventBus）
- [ ] 分布式事件总线实现（DistributedEventBus）
- [ ] 基于消息队列的事件传播实现
- [ ] 事件过滤器实现（EventFilter）
- [ ] 事件序列化和反序列化（EventSerializer）
- [ ] 自动配置支持（EventBusAutoConfiguration）
- [ ] 单元测试

#### plato-router 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 路由模型定义（Route、RouteRule、RouteTarget）
- [ ] 路由管理接口（RouterManager）
- [ ] 路由解析和匹配实现（RouteResolver）
- [ ] 路由策略实现（负载均衡、就近路由等）
- [ ] 路由缓存实现（RouteCache）
- [ ] 路由表动态更新机制
- [ ] REST API 接口实现（RouterController）
- [ ] 自动配置支持（RouterAutoConfiguration）
- [ ] 单元测试

#### plato-rpc 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] RPC 模型定义（RpcRequest、RpcResponse）
- [ ] 服务注册与发现集成
- [ ] 服务代理实现
- [ ] 客户端负载均衡实现
- [ ] 序列化和反序列化支持
- [ ] 拦截器链实现
- [ ] 限流和熔断机制
- [ ] 异步调用支持
- [ ] 自动配置支持（RpcAutoConfiguration）
- [ ] 单元测试

#### plato-logger 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 日志模型定义（LogEvent、LogLevel）
- [ ] 日志上下文实现（LogContext）
- [ ] 结构化日志支持
- [ ] 链路追踪集成
- [ ] MDC 增强支持
- [ ] 日志过滤器实现
- [ ] 日志异步处理
- [ ] 自动配置支持（LoggerAutoConfiguration）
- [ ] 单元测试

#### plato-id 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] ID 生成器接口设计（IdGenerator）
- [ ] 雪花算法实现（SnowflakeIdGenerator）
- [ ] 分段 ID 生成实现（SegmentIdGenerator）
- [ ] UUID 实现（UuidGenerator）
- [ ] ID 生成器工厂（IdGeneratorFactory）
- [ ] 分布式 ID 服务实现
- [ ] REST API 接口实现
- [ ] 自动配置支持（IdGeneratorAutoConfiguration）
- [ ] 单元测试

#### plato-retry 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 重试策略接口设计（RetryStrategy）
- [ ] 固定间隔重试策略实现（FixedIntervalRetryStrategy）
- [ ] 指数退避策略实现（ExponentialBackoffRetryStrategy）
- [ ] 重试上下文实现（RetryContext）
- [ ] 重试执行器实现（RetryExecutor）
- [ ] 注解支持（@Retryable）
- [ ] 异步重试支持
- [ ] 自动配置支持（RetryAutoConfiguration）
- [ ] 单元测试

### 业务模块实现

#### plato-gateway 模块
- [x] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 长连接管理
- [ ] 消息路由
- [ ] 与 state 模块交互
- [ ] 消息转发
- [ ] 单元测试

#### plato-state 模块
- [x] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 连接状态管理
- [ ] 心跳处理
- [ ] 重连机制
- [ ] 单元测试

#### plato-ipconf 模块
- [x] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] IP 列表管理
- [ ] 动态调度算法
- [ ] HTTP 接口
- [ ] 单元测试

#### plato-client 模块
- [x] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 命令行解析
- [ ] SDK 实现
- [ ] 用户界面
- [ ] 集成测试

#### plato-user 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 用户领域模型定义
- [ ] 用户服务接口设计
- [ ] 用户管理实现
- [ ] 认证授权实现
- [ ] 分布式用户存储实现
- [ ] REST API 接口实现
- [ ] 单元测试

#### plato-message-domain 模块
- [ ] 基础项目结构搭建
- [ ] Maven 依赖配置
- [ ] 消息领域模型定义
- [ ] 消息服务接口设计
- [ ] 消息存储实现
- [ ] 消息路由实现
- [ ] 消息推送实现
- [ ] REST API 接口实现
- [ ] 单元测试


### 跨功能任务

- [ ] 完善项目文档
- [ ] 代码风格统一
- [ ] CI/CD 流程完善
- [ ] 性能测试
- [ ] 安全测试
- [ ] 多环境部署配置
- [ ] 系统监控与告警
- [ ] 分布式追踪实现
- [ ] API 文档生成
- [ ] 系统压力测试
- [ ] 故障恢复测试

以上 TODO 列表将随着项目进展持续更新，已完成的任务会被标记为 [x]，待完成的任务标记为 [ ]。

### 2023 年 4 月 11 日
- 完成了 plato-discovery 模块的核心功能实现，包括：
    - 创建了服务注册和发现的核心接口（ServiceRegistry 和 ServiceDiscovery）
    - 实现了基于 Nacos 的服务注册（NacosServiceRegistry）和服务发现（NacosServiceDiscovery）
    - 设计了服务实例模型（ServiceInstance）和服务变更监听机制（ServiceChangeListener）
    - 实现了 REST API 控制器（ServiceRegistryController），提供服务管理接口
    - 实现了自动配置类（ServiceDiscoveryAutoConfiguration），简化模块使用
    - 完成了与 Spring Cloud Alibaba Nacos 的深度集成
    - 配置了 application.yml 和 bootstrap.yml，支持 Nacos 服务发现和配置中心
    - 编写了详细 README 文档，说明模块功能和使用方法

### 2023 年 4 月 15 日
- 完成了 plato-config 模块的核心功能实现，包括：
    - 设计并实现了配置模型（ConfigProperty 和 ConfigGroup）
    - 定义了配置服务接口（ConfigService）
    - 实现了配置变更监听器（ConfigChangeListener 和 LoggingConfigChangeListener）
    - 开发了本地内存配置服务实现（LocalConfigService）
    - 开发了基于 Nacos 的配置服务实现（NacosConfigService）
    - 实现了配置 REST API 接口（ConfigController）
    - 完成了 Spring Boot 自动配置集成（PlatoConfigAutoConfiguration）
    - 配置了 META-INF/spring.factories，支持自动配置
    - 编写了示例配置文件
    - 编写了详细的 README 文档
- 模块支持以下功能：
    - 配置的添加、修改、删除和查询
    - 配置的分组管理
    - 配置变更事件监听
    - 多种数据类型的配置值操作
    - 多种配置存储方式（本地内存和 Nacos）
    - 完整的 REST API 接口
    - 支持配置文件的导入和导出

### 2023 年 4 月 20 日
- 开始实现 plato-bizflow 模块，完成了以下内容：
    - 实现了核心接口和模型：Node 接口、NodeResult、ProcessContext、RetryStrategy
    - 完成了执行状态管理：ExecutionStatus 枚举
    - 设计并实现了节点执行器接口（NodeExecutor）及回调接口（ExecuteCallback）
    - 开发了完整的 DefaultNodeExecutor 实现，包含以下功能：
        - 基于 CompletableFuture 的异步执行机制
        - 节点超时控制
        - 基于策略的重试机制
        - 执行状态跟踪和统计
        - 执行前后的回调支持
        - 线程池资源管理
    - 自定义实现了 NamedThreadFactory，提供易于识别的线程命名
- 该模块采用轻量级设计，专注于提供业务流程编排的基础设施，与 Go 版本的设计保持一致性

### 2023 年 4 月 25 日
- 继续开发 plato-bizflow 模块，完成了以下内容：
    - 实现了图模型核心类：Edge 类（表示节点间的连接关系）、Graph 类（表示完整的 DAG 流程图）
    - 实现了拓扑排序算法（TopologicalSorter），用于确定节点的执行顺序
    - 实现了循环依赖检测和异常处理（CyclicDependencyException）
    - 开发了 GraphBuilder 类，提供流式 API 用于便捷构建 DAG 流程图
    - 设计并实现了 GraphExecutor 接口，定义了图执行器的行为
    - 完成了 DefaultGraphExecutor 实现，它能够：
        - 根据 DAG 图的拓扑结构并行执行节点
        - 管理节点之间的依赖关系，确保前置节点完成后再执行后续节点
        - 支持异步执行和同步执行
        - 支持按特定路径执行节点
        - 支持从特定节点开始执行
        - 提供执行状态跟踪和取消功能
        - 优雅处理异常和资源释放
- 至此，plato-bizflow 模块的核心功能已基本完成，后续将继续开发应用层服务和 REST API 接口

### 模块职责边界补充

#### plato-common 模块
以下是 plato-common 模块中已实现但在文档中未充分体现的功能：

1. **并发管理**
    - 线程池工具类（ThreadPoolUtils）：提供可配置的线程池创建和管理
    - 并发限制工具（ConcurrentLimiter）：实现 QPS 和并发度限制
    - 可重入读写锁封装（RWLock）：简化读写锁的使用

2. **数据结构**
    - LRU 缓存实现（LRUCache）：基于 LinkedHashMap 的 LRU 缓存
    - Trie 树实现（TrieTree）：用于高效的前缀匹配和搜索
    - 布隆过滤器（BloomFilter）：高效判断元素是否存在

3. **IO 工具**
    - 异步 IO 工具类（AsyncIOUtils）：提供非阻塞 IO 操作
    - 资源加载器（ResourceLoader）：统一的资源加载机制
    - 文件监听器（FileWatcher）：监控文件变化并触发回调

4. **扩展注解处理**
    - 注解处理器（AnnotationProcessor）：处理自定义注解
    - 切面支持（AspectSupport）：提供 AOP 功能支持
    - 条件注解（ConditionalOn*）：条件化配置支持

5. **系统安全**
    - 密码加密工具（PasswordEncoder）：密码安全加密和验证
    - 敏感数据脱敏（DataMasker）：敏感信息处理
    - 加密解密工具（CryptoUtils）：对称和非对称加密算法封装

#### plato-bizflow 模块
以下是 plato-bizflow 模块中已实现但在文档中未充分体现的功能：

1. **执行调度策略**
    - 并行度控制（ConcurrencyLimiter）：控制同时执行的节点数量
    - 优先级调度（PriorityScheduler）：支持节点优先级执行
    - 公平调度（FairScheduler）：确保资源公平分配

2. **错误处理机制**
    - 错误传播策略（ErrorPropagationStrategy）：控制错误如何影响后续节点
    - 补偿操作（CompensationOperation）：节点失败时的补偿逻辑
    - 错误恢复点（RecoveryPoint）：流程恢复执行的位置标记

3. **动态配置支持**
    - 运行时配置调整（RuntimeConfig）：允许在运行时调整执行参数
    - 动态节点加载（DynamicNodeLoader）：支持动态加载节点实现
    - 条件表达式引擎（ExpressionEngine）：用于边条件的复杂表达式评估

4. **监控与统计**
    - 执行指标收集（MetricsCollector）：收集流程执行的性能指标
    - 节点耗时统计（NodePerformanceTracker）：跟踪节点执行时间
    - 资源使用监控（ResourceMonitor）：监控执行过程中的资源使用情况

这些功能可以在后续开发中进一步完善和扩展，使 plato-bizflow 模块成为更加强大和灵活的业务流程编排引擎。

### 2023 年 4 月 15 日 - plato-storage 模块开发计划

#### 目标
实现 plato-storage 模块，提供统一的存储服务，支持消息和关系数据的存储、查询和管理。重点实现 Timeline 存储和缓存支持，为消息领域提供专业的存储基础设施。

#### 模块结构
```
plato-storage/
├── plato-storage-api/                           # 存储API模块
│   ├── src/main/java/
│   │   └── plato/storage/api/
│   │       ├── constant/                        # 常量定义
│   │       ├── enums/                           # 枚举定义
│   │       ├── model/                           # 数据模型
│   │       │   ├── relation/                    # 关系相关模型
│   │       │   └── message/                     # 消息相关模型
│   │       └── service/                         # 服务接口
│   └── pom.xml
│
├── plato-storage-core/                          # 存储核心实现
│   ├── src/main/java/
│   │   └── plato/storage/core/
│   │       ├── config/                          # 配置类
│   │       ├── repository/                      # 数据访问层
│   │       ├── entity/                          # 数据库实体
│   │       ├── mapper/                          # MyBatis映射
│   │       └── service/                         # 服务实现
│   ├── src/main/resources/
│   │   ├── mapper/                             # MyBatis XML映射
│   │   └── schema/                             # 数据库Schema
│   └── pom.xml
│
└── pom.xml                                      # 父POM文件
```

#### 开发计划（预计 6 周）

##### 第 1-2 周：Timeline 存储接口设计
- [ ] 存储接口定义
    - StorageService 存储服务接口
    - MessageStorage 消息存储接口
    - RelationStorage 关系存储接口
    - TimelineStorage 时间线存储接口
- [ ] 数据模型定义
    - UserRelation 用户关系模型
    - DeviceRelation 设备关系模型
    - SessionRelation 会话关系模型
    - Message 消息模型
    - MessageState 消息状态模型
- [ ] 查询条件定义
    - QueryCriteria 查询条件接口
    - MessageQuery 消息查询条件
    - RelationQuery 关系查询条件
    - TimelineQuery 时间线查询条件

##### 第 3-4 周：核心缓存和 Timeline 实现
- [ ] 核心存储实现
    - AbstractStorageService 抽象存储服务
    - DefaultMessageStorage 默认消息存储
    - DefaultRelationStorage 默认关系存储
    - DefaultTimelineStorage 默认时间线存储
- [ ] 缓存支持实现
    - 旁路缓存模式（对关系数据）
    - 穿透缓存模式（对离线消息）
    - 只读缓存模式（对配置数据）
- [ ] 索引管理
    - IndexManager 索引管理器
    - MessageIndex 消息索引
    - TimelineIndex 时间线索引

##### 第 5-6 周：优化和集成
- [ ] 冷热数据访问优化
    - 热数据缓存机制
    - 冷数据批量加载
- [ ] 消息拉取优化
    - 分页拉取机制
    - 增量拉取算法
    - 消息状态追踪
- [ ] 离线消息处理
    - 消息缓存机制
    - 消息推送重试逻辑
    - 消息确认机制

#### 具体实现要点

##### 1. 缓存支持
- **在现有 MySQL 基础上增加缓存层**：
    - 实现旁路缓存模式（对关系数据）
    - 实现穿透缓存模式（对离线消息）
    - 实现只读缓存模式（对配置数据）
- **缓存高可用**：
    - 实现简单的防缓存穿透机制
    - 实现缓存预热功能
    - 添加缓存统计和监控

##### 2. Timeline 存储实现
- **基于 MySQL 实现**：
    - 使用现有的 msg 表作为基础
    - 增加高效的索引和查询优化
    - 实现基于 seqID 的消息顺序管理
- **消息拉取优化**：
    - 实现分页拉取机制
    - 开发增量拉取算法
    - 实现消息状态追踪

#### 依赖关系

##### 内部依赖
- plato-storage-api：基础 API 定义
- plato-storage-core：实现和 MySQL 交互

##### 外部依赖
- MySQL：核心存储引擎
- Redis：缓存支持
- Spring 框架：提供事务和集成支持

#### 未来演进规划（非当前开发计划）
1. **分布式 KV 存储**：未来可考虑自研基于 LSM 树的 KV 存储
2. **高级 Timeline 功能**：未来可实现更复杂的冷热数据分离
3. **扩展存储能力**：可扩展支持分库分表和 NoSQL 集成











## Plato-Java 中台化架构重构





### 最新架构



```
plato/
├── docs/                                # 项目文档
│   ├── architecture/                    # 架构设计文档
│   ├── api/                            # API文档
│   └── deployment/                     # 部署文档
│
├── plato-parent/                        # 父项目，管理依赖版本和构建配置
│   └── pom.xml                          # 父POM，定义依赖版本和插件
│
├── plato-bom/                           # Bill of Materials
│   └── pom.xml                          # 依赖版本管理
│
├── plato-framework/                     # 框架核心模块
│   │
│   ├── plato-framework-common/          # 通用工具和基础类
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/common/
│   │   │       ├── constants/          # 常量定义
│   │   │       ├── enums/              # 枚举定义
│   │   │       ├── utils/              # 工具类
│   │   │       └── model/              # 通用数据模型
│   │   └── pom.xml
│   │
│   ├── plato-framework-protocol/        # 协议定义和序列化
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/protocol/
│   │   │       ├── message/            # 消息定义
│   │   │       ├── codec/              # 编解码器
│   │   │       ├── serializer/         # 序列化器
│   │   │       └── transport/          # 传输层抽象
│   │   ├── src/main/resources/
│   │   │   └── proto/                 # Protocol Buffers定义
│   │   └── pom.xml
│   │
│   ├── plato-framework-discovery/       # 服务发现抽象
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/discovery/
│   │   │       ├── api/                # 服务发现API
│   │   │       ├── event/              # 服务事件
│   │   │       └── registry/           # 注册中心抽象
│   │   └── pom.xml
│   │
│   ├── plato-framework-spring/          # Spring集成支持
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/spring/
│   │   │       ├── config/             # 自动配置
│   │   │       ├── context/            # Spring上下文支持
│   │   │       └── annotation/         # 注解支持
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 框架模块POM
│
├── plato-infrastructure/                # 基础设施模块（独立）
│   ├── plato-infrastructure-api/        # 基础设施API定义
│   │   ├── src/main/java/
│   │   │   └── com/plato/infrastructure/api/
│   │   │       ├── cache/              # 缓存接口
│   │   │       ├── config/             # 配置接口
│   │   │       ├── lock/               # 锁接口
│   │   │       ├── mq/                 # 消息队列接口
│   │   │       └── storage/            # 存储接口
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-cache/      # 缓存中间件集成
│   │   ├── plato-infrastructure-cache-api/      # 缓存API
│   │   ├── plato-infrastructure-cache-redis/    # Redis缓存实现
│   │   ├── plato-infrastructure-cache-caffeine/ # Caffeine本地缓存
│   │   ├── plato-infrastructure-cache-multilevel/ # 多级缓存
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-config/     # 配置中心集成
│   │   ├── plato-infrastructure-config-api/     # 配置API
│   │   ├── plato-infrastructure-config-nacos/   # Nacos配置实现
│   │   ├── plato-infrastructure-config-spring/  # Spring配置实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-lock/       # 分布式锁
│   │   ├── plato-infrastructure-lock-api/       # 锁API
│   │   ├── plato-infrastructure-lock-redis/     # Redis分布式锁
│   │   ├── plato-infrastructure-lock-zookeeper/ # Zookeeper锁
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-mq/         # 消息队列集成
│   │   ├── plato-infrastructure-mq-api/        # 消息队列API
│   │   ├── plato-infrastructure-mq-kafka/      # Kafka实现
│   │   ├── plato-infrastructure-mq-rocketmq/   # RocketMQ实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-db/         # 数据库中间件
│   │   ├── plato-infrastructure-db-api/        # 数据访问API
│   │   ├── plato-infrastructure-db-mybatis/    # MyBatis集成
│   │   ├── plato-infrastructure-db-jpa/        # JPA集成
│   │   ├── plato-infrastructure-db-sharding/   # 分库分表
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-scheduler/  # 调度中间件
│   │   ├── plato-infrastructure-scheduler-api/    # 调度API
│   │   ├── plato-infrastructure-scheduler-quartz/ # Quartz集成
│   │   ├── plato-infrastructure-scheduler-xxljob/ # XXL-Job集成
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-id/         # ID生成器
│   │   ├── plato-infrastructure-id-api/         # ID生成API
│   │   ├── plato-infrastructure-id-leaf/        # Leaf实现
│   │   ├── plato-infrastructure-id-snowflake/   # 雪花算法实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-storage/    # 存储中间件
│   │   ├── plato-infrastructure-storage-api/    # 存储API
│   │   ├── plato-infrastructure-storage-oss/    # 对象存储
│   │   ├── plato-infrastructure-storage-minio/  # MinIO实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-resilience/ # 弹性能力
│   │   ├── plato-infrastructure-resilience-api/       # 弹性API
│   │   ├── plato-infrastructure-resilience-r4j/       # Resilience4j实现
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 基础设施模块POM
│
├── plato-integration/                   # 集成模块
│   ├── plato-integration-spring/        # Spring集成
│   ├── plato-integration-dubbo/         # Dubbo集成
│   ├── plato-integration-sentinel/      # Sentinel集成
│   └── pom.xml
│
├── plato-access/                        # 接入层模块（长连接服务）
│   ├── plato-access-api/                # 接入层API
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/api/
│   │   │       ├── model/              # 接入层数据模型
│   │   │       ├── exception/          # 接入层异常
│   │   │       └── service/            # 接入层服务接口
│   │   └── pom.xml
│   │
│   ├── plato-access-gateway/            # 网关服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/gateway/
│   │   │       ├── config/             # 配置
│   │   │       ├── handler/            # 处理器
│   │   │       ├── netty/              # Netty实现
│   │   │       ├── filter/             # 过滤器
│   │   │       ├── util/               # 工具类
│   │   │       └── service/            # 服务实现
│   │   ├── src/main/resources/
│   │   │   ├── application.yml         # 应用配置
│   │   │   └── bootstrap.yml           # 启动配置
│   │   └── pom.xml
│   │
│   ├── plato-access-ipconf/             # IP配置服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/ipconf/
│   │   │       ├── config/             # 配置
│   │   │       ├── controller/         # 控制器
│   │   │       ├── service/            # 服务实现
│   │   │       └── repository/         # 数据访问
│   │   ├── src/main/resources/
│   │   └── pom.xml
│   │
│   ├── plato-access-state/              # 状态服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/state/
│   │   │       ├── config/             # 配置
│   │   │       ├── handler/            # 处理器
│   │   │       ├── processor/          # 消息处理器
│   │   │       ├── service/            # 服务实现
│   │   │       └── repository/         # 数据访问
│   │   ├── src/main/resources/
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 接入层模块POM
│
├── plato-domain/                        # 领域服务模块
│   ├── plato-domain-core/               # 领域核心
│   │   ├── src/main/java/
│   │   │   └── com/plato/domain/core/
│   │   │       ├── annotation/         # 领域注解
│   │   │       ├── exception/          # 领域异常
│   │   │       ├── model/              # 领域模型基类
│   │   │       ├── repository/         # 仓储接口
│   │   │       └── service/            # 服务接口
│   │   └── pom.xml
│   │
│   ├── plato-domain-user/               # 用户领域服务
│   │   ├── plato-domain-user-api/        # 用户领域API
│   │   ├── plato-domain-user-core/       # 用户领域核心
│   │   ├── plato-domain-user-infrastructure/ # 用户领域基础设施
│   │   └── pom.xml
│   │
│   ├── plato-domain-message/            # 消息领域服务
│   │   ├── plato-domain-message-api/
│   │   ├── plato-domain-message-core/
│   │   ├── plato-domain-message-infrastructure/
│   │   └── pom.xml
│   │
│   ├── plato-domain-session/            # 会话领域服务
│   │   ├── plato-domain-session-api/
│   │   ├── plato-domain-session-core/
│   │   ├── plato-domain-session-infrastructure/
│   │   └── pom.xml
│   │
│   ├── plato-domain-relation/           # 关系领域服务
│   │   ├── plato-domain-relation-api/
│   │   ├── plato-domain-relation-core/
│   │   ├── plato-domain-relation-infrastructure/
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 领域服务模块POM
│
├── plato-adapter/                       # 业务适配层
│   ├── plato-adapter-api/               # 适配层API
│   ├── plato-adapter-im/                # IM业务适配
│   ├── plato-adapter-push/              # 推送业务适配
│   └── pom.xml
│
├── plato-event-center/                  # 事件中心
│   ├── plato-event-center-api/          # 事件中心API
│   ├── plato-event-center-core/         # 事件中心核心实现
│   ├── plato-event-center-processor/    # 事件处理器
│   └── pom.xml
│
├── plato-storage/                       # 存储服务模块
│   ├── plato-storage-api/               # 存储API
│   ├── plato-storage-core/              # 存储核心实现
│   ├── plato-storage-oss/               # 对象存储实现
│   └── pom.xml
│
├── plato-client-sdk/                    # 客户端SDK
│   ├── plato-client-sdk-core/           # SDK核心
│   ├── plato-client-sdk-java/           # Java客户端
│   ├── plato-client-sdk-android/        # Android客户端
│   └── pom.xml
│
├── plato-api/                           # API网关
│   ├── plato-api-core/                  # API核心
│   ├── plato-api-rest/                  # REST API实现
│   ├── plato-api-grpc/                  # gRPC API实现
│   └── pom.xml
│
├── plato-bizflow/                       # 业务流程引擎（自研核心模块）
│   ├── plato-bizflow-api/               # 流程引擎API
│   ├── plato-bizflow-core/              # 流程引擎核心
│   │   ├── src/main/java/
│   │   │   └── com/plato/bizflow/core/
│   │   │       ├── engine/             # 引擎实现
│   │   │       ├── model/              # 流程模型
│   │   │       ├── execution/          # 执行器
│   │   │       ├── parser/             # 流程解析器
│   │   │       └── storage/            # 流程存储
│   │   └── pom.xml
│   │
│   ├── plato-bizflow-dsl/               # 流程DSL
│   ├── plato-bizflow-spring/            # Spring集成
│   └── pom.xml
│
├── plato-observability/                 # 可观察性模块
│   ├── plato-observability-api/         # 可观察性API
│   ├── plato-observability-actuator/    # Spring Boot Actuator集成
│   ├── plato-observability-micrometer/  # Micrometer指标收集
│   │   ├── src/main/java/
│   │   │   └── com/plato/observability/micrometer/
│   │   │       ├── config/              # 配置
│   │   │       ├── metrics/             # 指标定义
│   │   │       ├── binder/              # 指标绑定器
│   │   │       └── exporter/            # 指标导出器
│   │   └── pom.xml
│   │
│   ├── plato-observability-tracing/     # 分布式追踪
│   ├── plato-observability-logging/     # 日志增强
│   ├── plato-observability-dashboard/   # 监控仪表盘
│   └── pom.xml
│
├── plato-boot/                          # 启动模块
│   ├── plato-boot-core/                 # 核心启动功能
│   │   ├── src/main/java/
│   │   │   └── com/plato/boot/core/
│   │   │       ├── launcher/            # 启动器
│   │   │       ├── config/              # 配置加载
│   │   │       ├── lifecycle/           # 生命周期管理
│   │   │       └── listener/            # 事件监听器
│   │   └── pom.xml
│   │
│   ├── plato-boot-config/               # 配置加载
│   ├── plato-boot-cli/                  # 命令行工具
│   └── pom.xml
│
├── plato-web/                          # Web应用模块
│   ├── plato-web-admin/                # 管理后台
│   ├── plato-web-portal/               # 用户门户
│   └── pom.xml
│
├── plato-example/                      # 示例模块
│   ├── plato-example-basic/            # 基础使用示例
│   ├── plato-example-advanced/         # 高级特性示例
│   └── pom.xml
│
├── plato-ops/                          # 运维模块
│   ├── plato-ops-deploy/               # 部署脚本
│   ├── plato-ops-monitor/              # 监控配置
│   ├── plato-ops-alert/                # 告警配置
│   └── docker/                         # Docker配置
│       ├── Dockerfile                  # 应用Dockerfile
│       └── docker-compose.yml          # 本地开发环境
│
├── .gitignore                          # Git忽略配置
├── mvnw                                # Maven包装器
├── mvnw.cmd                            # Maven包装器(Windows)
├── pom.xml                             # 根POM
└── README.md                           # 项目说明
```





基于对 Go 项目功能分析，我们将分阶段实现 Java 版 Plato 系统，首先专注于核心功能的复现，然后再扩展其他模块。

### 第一阶段：核心基础设施实现

这个阶段专注于构建基础框架，确保后续各模块能正常工作。

#### 1. 基础框架模块 (plato-framework)

**1.1 plato-common**

- 异常体系设计
- 通用工具类实现(日志、时间、字符串处理等)
- 通用模型定义

**1.2 plato-protocol**

- 消息协议定义(参考 Go 的自定义二进制协议)
- 编解码器实现
- 消息头和消息体结构

**1.3 plato-discovery**

- 服务注册接口
- 服务发现接口
- Nacos/ZooKeeper 实现

**1.4 plato-infrastructure**

- 配置管理(对应 Go 的 common/config)
- 缓存实现(对应 Go 的 common/cache)
- 客户端调用(对应 Go 的 common/prpc)

#### 2. 启动与配置模块 (plato-boot)

- 配置加载(对应 Go 的 config.Init)
- 启动入口(对应 Go 的 cmd 模块)
- 服务生命周期管理

### 第二阶段：核心接入层实现

这个阶段实现的是系统的接入层，负责管理客户端连接。

#### 3. 接入层模块 (plato-access)

**3.1 plato-gateway**

- 长连接服务器(使用 Netty 实现，对应 Go 的 gateway)
- 连接管理(对应 Go 的 connection.go 和 epoll_linux.go)
- 消息路由(对应 Go 的 router)
- 工作池实现(对应 Go 的 workpool.go)

**3.2 plato-state**

- 状态服务实现(对应 Go 的 state)
- 会话管理(对应 Go 的 state.go)
- 心跳机制(对应 Go 的 timer.go)
- 状态缓存(对应 Go 的 cache.go)

**3.3 plato-ipconf**

- IP 配置服务(对应 Go 的 ipconf)
- 负载均衡算法(对应 Go 的 domain/ipconf)
- 服务健康检查
- 地理位置服务

### 第三阶段：客户端 SDK 实现

这个阶段实现客户端 SDK，便于业务系统接入 Plato 系统。

#### 4. 客户端 SDK 模块 (plato-client-sdk)

**4.1 plato-client-core**

- 连接管理
- 认证鉴权
- 消息发送与接收

**4.2 plato-client-message**

- 消息类型定义
- 消息处理
- 消息 ACK 机制

### 第四阶段：性能监控实现

这个阶段实现性能监控，确保系统运行状态可观测。

#### 5. 性能监控模块 (plato-perf)

- 性能指标收集
- 性能数据处理
- 监控报告生成

### 第五阶段：领域层基础实现

这个阶段实现最基本的领域服务。

#### 6. 领域服务模块 (plato-domain)

**6.1 plato-domain-user**

- 用户领域模型
- 用户服务接口
- 用户认证逻辑

### 实现优先级和顺序

为了确保系统能尽快运行起来，建议按照以下顺序实现：

1. **最高优先级** (系统运行的基础)
    - plato-protocol (网络协议)
    - plato-discovery (服务发现)
    - plato-boot (启动配置)
    - plato-gateway (网关服务)

2. **高优先级** (系统核心功能)
    - plato-state (状态服务)
    - plato-ipconf (负载均衡)
    - plato-client-core (核心 SDK)

3. **中优先级** (功能完善)
    - plato-infrastructure (基础设施)
    - plato-client-message (消息 SDK)
    - plato-domain-user (用户服务)

4. **低优先级** (监控与优化)
    - plato-perf (性能监控)

### 技术实现对照表

| Go 项目组件      | Java 实现技术选型         |
| --------------- | ------------------------ |
| epoll/iocp      | Netty EventLoop          |
| Protobuf        | Protobuf-Java            |
| etcd            | Nacos/ZooKeeper          |
| gRPC            | gRPC-Java/Spring Cloud   |
| Goroutine 工作池 | 线程池/虚拟线程(Java 21) |
| 时间轮算法      | HashedWheelTimer         |
| 命令行工具      | Spring Shell/Picocli     |
| 缓存实现        | Redis + Spring Cache     |
| 日志系统        | SLF4J + Logback          |

### 核心模块开发计划

### 第一阶段：基础框架模块 (plato-framework)

#### 1. plato-common 模块 (1 周)

- [ ] 异常体系设计
    - PlatoException 基类
    - BusinessException 业务异常
    - SystemException 系统异常
    - NetworkException 网络异常
- [ ] 通用工具类实现
    - HashUtils 哈希工具
    - StringUtils 字符串处理
    - DateUtils 日期处理
    - CollectionUtils 集合处理
- [ ] 通用模型定义
    - Result 统一返回结果
    - Page 分页模型
    - BaseEntity 基础实体

#### 2. plato-protocol 模块 (1 周)

- [ ] 消息协议定义
    - 消息头结构
    - 消息体结构
    - 消息类型枚举
- [ ] 编解码器实现
    - MessageEncoder 消息编码器
    - MessageDecoder 消息解码器
    - MessageCodec 消息编解码器
- [ ] 序列化实现
    - Protobuf 序列化器
    - JSON 序列化器
    - 自定义二进制序列化器

#### 3. plato-discovery 模块 (1 周)

- [ ] 服务发现接口
    - ServiceRegistry 服务注册
    - ServiceDiscovery 服务发现
    - ServiceInstance 服务实例
- [ ] Nacos 实现
    - NacosServiceRegistry
    - NacosServiceDiscovery
    - NacosServiceInstance
- [ ] 服务健康检查
    - HealthCheck 接口
    - DefaultHealthCheck 实现

#### 4. plato-infrastructure 模块 (2 周)

- [ ] 配置管理
    - ConfigService 配置服务
    - LocalConfigService 本地配置
    - NacosConfigService 远程配置
- [ ] 缓存实现
    - CacheService 缓存服务
    - RedisCacheService 实现
    - LocalCacheService 实现
- [ ] 客户端调用
    - RpcClient 接口
    - GrpcClient 实现
    - RestClient 实现

### 第二阶段：接入层模块 (plato-access)

#### 1. plato-gateway 模块 (2 周)

- [ ] Netty 服务器实现
    - ServerBootstrap 配置
    - ChannelHandler 实现
    - MessageHandler 实现
- [ ] 连接管理
    - ConnectionManager 接口
    - DefaultConnectionManager 实现
    - ConnectionPool 连接池
- [ ] 消息路由
    - Router 接口
    - DefaultRouter 实现
    - RouteRule 路由规则

#### 2. plato-state 模块 (2 周)

- [ ] 状态服务实现
    - StateService 接口
    - DefaultStateService 实现
    - StateRepository 存储
- [ ] 会话管理
    - SessionManager 接口
    - DefaultSessionManager 实现
    - SessionRepository 存储
- [ ] 心跳机制
    - HeartbeatService 接口
    - DefaultHeartbeatService 实现
    - HeartbeatMonitor 监控

#### 3. plato-ipconf 模块 (1 周)

- [ ] IP 配置服务
    - IpConfService 接口
    - DefaultIpConfService 实现
    - IpConfRepository 存储
- [ ] 负载均衡
    - LoadBalancer 接口
    - RoundRobinLoadBalancer 实现
    - WeightedLoadBalancer 实现
- [ ] 健康检查
    - HealthChecker 接口
    - DefaultHealthChecker 实现
    - HealthCheckMonitor 监控

### 开发规范

1. **代码规范**
    - 遵循阿里巴巴 Java 开发手册
    - 使用 CheckStyle 进行代码风格检查
    - 使用 SpotBugs 进行代码质量检查

2. **测试规范**
    - 每个模块必须包含单元测试
    - 核心功能必须包含集成测试
    - 测试覆盖率要求达到 80%以上

3. **文档规范**
    - 每个模块必须包含 README.md
    - 核心接口必须有 JavaDoc 注释
    - 复杂逻辑必须有详细注释

4. **版本控制**
    - 使用 Git Flow 工作流
    - 每个功能使用 feature 分支
    - 提交信息必须清晰描述变更内容

### 开发流程

1. **需求分析**
    - 分析 Go 项目对应模块的功能
    - 设计 Java 版本的实现方案
    - 编写详细设计文档

2. **开发实现**
    - 创建模块基础结构
    - 实现核心功能
    - 编写单元测试
    - 进行代码审查

3. **集成测试**
    - 模块间集成测试
    - 性能测试
    - 压力测试

4. **文档完善**
    - 更新 API 文档
    - 编写使用示例
    - 更新项目文档

### 时间安排

- 第一阶段：5 周
    - plato-common: 1 周
    - plato-protocol: 1 周
    - plato-discovery: 1 周
    - plato-infrastructure: 2 周

- 第二阶段：5 周
    - plato-gateway: 2 周
    - plato-state: 2 周
    - plato-ipconf: 1 周

总计：10 周完成核心模块开发

### 风险控制

1. **技术风险**
    - Netty 性能优化
    - 分布式一致性
    - 高并发处理

2. **进度风险**
    - 模块间依赖关系
    - 技术难点攻关
    - 测试覆盖率要求

3. **质量风险**
    - 代码质量保证
    - 性能指标达标
    - 系统稳定性

### 监控指标

1. **开发进度**
    - 模块完成度
    - 测试覆盖率
    - 代码审查通过率

2. **代码质量**
    - 代码规范符合度
    - 代码重复率
    - 圈复杂度

3. **性能指标**
    - 响应时间
    - 并发处理能力
    - 资源使用率

### 第三阶段：存储模块实现 (plato-storage)

#### 1. plato-storage-api 模块 (1 周)

- [ ] 存储接口定义
    - StorageService 存储服务接口
    - MessageStorage 消息存储接口
    - RelationStorage 关系存储接口
    - TimelineStorage 时间线存储接口
- [ ] 数据模型定义
    - UserRelation 用户关系模型
    - DeviceRelation 设备关系模型
    - SessionRelation 会话关系模型
    - Message 消息模型
    - MessageState 消息状态模型
- [ ] 查询条件定义
    - QueryCriteria 查询条件接口
    - MessageQuery 消息查询条件
    - RelationQuery 关系查询条件
    - TimelineQuery 时间线查询条件

#### 2. plato-storage-core 模块 (2 周)

- [ ] 核心存储实现
    - AbstractStorageService 抽象存储服务
    - DefaultMessageStorage 默认消息存储
    - DefaultRelationStorage 默认关系存储
    - DefaultTimelineStorage 默认时间线存储
- [ ] 存储策略实现
    - StorageStrategy 存储策略接口
    - HotColdStorageStrategy 冷热存储策略
    - ShardingStrategy 分片策略
    - ReplicationStrategy 复制策略
- [ ] 索引管理
    - IndexManager 索引管理器
    - MessageIndex 消息索引
    - TimelineIndex 时间线索引
    - RelationIndex 关系索引

#### 3. plato-storage-rdb 模块 (2 周)

- [ ] 关系表设计与实现
    - UserToUserTable 用户关系表
    - UserToDeviceTable 用户设备表
    - UserToSessionTable 用户会话表
    - MessageTable 消息表
    - MessageStateTable 消息状态表
- [ ] MyBatis 映射实现
    - UserRelationMapper 用户关系映射
    - DeviceRelationMapper 设备关系映射
    - SessionRelationMapper 会话关系映射
    - MessageMapper 消息映射
    - MessageStateMapper 状态映射
- [ ] 数据访问层实现
    - UserRelationRepository 用户关系仓储
    - DeviceRelationRepository 设备关系仓储
    - SessionRelationRepository 会话关系仓储
    - MessageRepository 消息仓储
    - MessageStateRepository 状态仓储

#### 4. plato-storage-timeline 模块 (2 周)

- [ ] 时间线核心实现
    - TimelineService 时间线服务
    - TimelineSegment 时间线分段
    - TimelineIterator 时间线迭代器
- [ ] 消息同步机制
    - MessageSyncService 消息同步服务
    - SyncStrategy 同步策略
    - ConflictResolver 冲突解决器
- [ ] 消息拉取实现
    - MessagePullService 消息拉取服务
    - PullStrategy 拉取策略
    - BatchProcessor 批处理器

### 开发规范补充

1. **数据库设计规范**
    - 遵循数据库设计三范式
    - 合理使用索引
    - 分表分库策略明确
    - 字段命名规范统一

2. **存储性能规范**
    - 批量操作优化
    - 索引覆盖优化
    - 读写分离策略
    - 缓存优化策略

### 时间安排补充

- 第三阶段：7 周
    - plato-storage-api: 1 周
    - plato-storage-core: 2 周
    - plato-storage-rdb: 2 周
    - plato-storage-timeline: 2 周

### 风险控制补充

1. **存储风险**
    - 数据一致性保证
    - 高并发写入处理
    - 大数据量查询优化
    - 数据安全保护

2. **性能风险**
    - 慢查询优化
    - 索引效率
    - 存储空间增长
    - 读写性能平衡

### 监控指标补充

1. **存储指标**
    - 数据库性能指标
    - 存储空间使用率
    - 查询响应时间
    - 写入吞吐量

2. **业务指标**
    - 消息存储延迟
    - 关系查询性能
    - 时间线拉取效率
    - 数据同步延迟

### 测试要求补充

1. **存储测试**
    - 数据一致性测试
    - 并发压力测试
    - 容量测试
    - 故障恢复测试

2. **性能测试**
    - 读写性能基准测试
    - 大数据量测试
    - 复杂查询性能测试
    - 批量操作性能测试







### plato-storage 模块详细目录结构

```
plato-storage/
├── plato-storage-api/                           # 存储API模块
│   ├── src/main/java/
│   │   └── com/plato/storage/api/
│   │       ├── constant/                        # 常量定义
│   │       │   ├── StorageConstants.java        # 存储相关常量
│   │       │   └── StorageErrorCode.java        # 错误码定义
│   │       ├── enums/                           # 枚举定义
│   │       │   ├── MessageType.java            # 消息类型
│   │       │   └── MessageStatus.java          # 消息状态
│   │       ├── model/                           # 数据模型
│   │       │   ├── relation/                    # 关系相关模型
│   │       │   │   ├── UserToUser.java         # 用户-用户关系
│   │       │   │   ├── UserToDevice.java       # 用户-设备关系
│   │       │   │   └── UserToSession.java      # 用户-会话关系
│   │       │   └── message/                     # 消息相关模型
│   │       │       ├── Message.java            # 消息实体
│   │       │       └── MessageState.java       # 消息状态
│   │       └── service/                         # 服务接口
│   │           ├── RelationStorage.java        # 关系存储接口
│   │           └── MessageStorage.java         # 消息存储接口
│   └── pom.xml
│
├── plato-storage-core/                          # 存储核心实现
│   ├── src/main/java/
│   │   └── com/plato/storage/core/
│   │       ├── config/                          # 配置类
│   │       │   ├── StorageAutoConfiguration.java # 自动配置
│   │       │   ├── StorageProperties.java      # 配置属性
│   │       │   └── MybatisConfiguration.java   # MyBatis配置
│   │       ├── repository/                      # 数据访问层
│   │       │   ├── relation/                    # 关系仓储
│   │       │   │   ├── UserToUserRepository.java
│   │       │   │   ├── UserToDeviceRepository.java
│   │       │   │   └── UserToSessionRepository.java
│   │       │   └── message/                     # 消息仓储
│   │       │       ├── MessageRepository.java
│   │       │       └── MessageStateRepository.java
│   │       ├── entity/                          # 数据库实体
│   │       │   ├── relation/                    # 关系实体
│   │       │   │   ├── UserToUserDO.java
│   │       │   │   ├── UserToDeviceDO.java
│   │       │   │   └── UserToSessionDO.java
│   │       │   └── message/                     # 消息实体
│   │       │       ├── MessageDO.java
│   │       │       └── MessageStateDO.java
│   │       ├── mapper/                          # MyBatis映射
│   │       │   ├── relation/                    # 关系Mapper
│   │       │   │   ├── UserToUserMapper.java
│   │       │   │   ├── UserToDeviceMapper.java
│   │       │   │   └── UserToSessionMapper.java
│   │       │   └── message/                     # 消息Mapper
│   │       │       ├── MessageMapper.java
│   │       │       └── MessageStateMapper.java
│   │       └── service/                         # 服务实现
│   │           ├── RelationStorageImpl.java    # 关系存储实现
│   │           └── MessageStorageImpl.java     # 消息存储实现
│   ├── src/main/resources/
│   │   ├── mapper/                             # MyBatis XML映射
│   │   │   ├── relation/
│   │   │   │   ├── UserToUserMapper.xml
│   │   │   │   ├── UserToDeviceMapper.xml
│   │   │   │   └── UserToSessionMapper.xml
│   │   │   └── message/
│   │   │       ├── MessageMapper.xml
│   │   │       └── MessageStateMapper.xml
│   │   └── schema/                            # 数据库Schema
│   │       ├── init.sql                       # 初始化SQL
│   │       └── tables.sql                     # 建表SQL
│   └── pom.xml
│
└── pom.xml                                      # 父POM文件
```

### 模块说明

1. **plato-storage-api**
    - 定义存储相关的接口和模型
    - 包含关系存储和消息存储的核心接口
    - 定义数据模型和查询接口

2. **plato-storage-core**
    - 实现核心存储逻辑
    - 提供基础的数据访问层实现
    - 管理数据库实体和映射
    - 包含 MyBatis 相关实现和配置
    - 包含数据库表结构和初始化脚本

3. **plato-storage-mysql**
    - MySQL 具体实现
    - MyBatis 映射和 SQL 定义
    - 数据库表结构和初始化脚本

### 关键文件说明

1. **数据模型**
    - `UserToUser.java`: 用户-用户关系模型
    - `UserToDevice.java`: 用户-设备关系模型
    - `UserToSession.java`: 用户-会话关系模型
    - `Message.java`: 消息实体模型
    - `MessageState.java`: 消息状态模型

2. **存储接口**
    - `RelationStorage.java`: 关系存储接口
    - `MessageStorage.java`: 消息存储接口

3. **数据库表**
    - `tables.sql`: 包含所有表的建表语句
        - user_to_user 表
        - user_to_device 表
        - user_to_session 表
        - msg 表
        - msg_state 表

4. **数据访问层**
    - `UserToUserRepository.java`: 用户关系仓储
    - `MessageRepository.java`: 消息仓储
    - `MessageStateRepository.java`: 消息状态仓储

### 2023 年 5 月 1 日 - plato-storage 模块开发计划

#### 目标

实现 plato-storage 模块，提供统一的存储服务，支持消息和关系数据的存储、查询和管理。

#### 模块详细目录结构

```
plato-storage/
├── plato-storage-api/                           # 存储API模块
│   ├── src/main/java/
│   │   └── com/plato/storage/api/
│   │       ├── constant/                        # 常量定义
│   │       │   ├── StorageConstants.java        # 存储相关常量
│   │       │   └── StorageErrorCode.java        # 错误码定义
│   │       ├── enums/                           # 枚举定义
│   │       │   ├── MessageType.java            # 消息类型
│   │       │   └── MessageStatus.java          # 消息状态
│   │       ├── model/                           # 数据模型
│   │       │   ├── relation/                    # 关系相关模型
│   │       │   │   ├── UserToUser.java         # 用户-用户关系
│   │       │   │   ├── UserToDevice.java       # 用户-设备关系
│   │       │   │   └── UserToSession.java      # 用户-会话关系
│   │       │   └── message/                     # 消息相关模型
│   │       │       ├── Message.java            # 消息实体
│   │       │       └── MessageState.java       # 消息状态
│   │       └── service/                         # 服务接口
│   │           ├── RelationStorage.java        # 关系存储接口
│   │           └── MessageStorage.java         # 消息存储接口
│   └── pom.xml
│
├── plato-storage-core/                          # 存储核心实现
│   ├── src/main/java/
│   │   └── com/plato/storage/core/
│   │       ├── config/                          # 配置类
│   │       │   ├── StorageAutoConfiguration.java # 自动配置
│   │       │   ├── StorageProperties.java      # 配置属性
│   │       │   └── MybatisConfiguration.java   # MyBatis配置
│   │       ├── repository/                      # 数据访问层
│   │       │   ├── relation/                    # 关系仓储
│   │       │   │   ├── UserToUserRepository.java
│   │       │   │   ├── UserToDeviceRepository.java
│   │       │   │   └── UserToSessionRepository.java
│   │       │   └── message/                     # 消息仓储
│   │       │       ├── MessageRepository.java
│   │       │       └── MessageStateRepository.java
│   │       ├── entity/                          # 数据库实体
│   │       │   ├── relation/                    # 关系实体
│   │       │   │   ├── UserToUserDO.java
│   │       │   │   ├── UserToDeviceDO.java
│   │       │   │   └── UserToSessionDO.java
│   │       │   └── message/                     # 消息实体
│   │       │       ├── MessageDO.java
│   │       │       └── MessageStateDO.java
│   │       ├── mapper/                          # MyBatis映射
│   │       │   ├── relation/                    # 关系Mapper
│   │       │   │   ├── UserToUserMapper.java
│   │       │   │   ├── UserToDeviceMapper.java
│   │       │   │   └── UserToSessionMapper.java
│   │       │   └── message/                     # 消息Mapper
│   │       │       ├── MessageMapper.java
│   │       │       └── MessageStateMapper.java
│   │       └── service/                         # 服务实现
│   │           ├── RelationStorageImpl.java    # 关系存储实现
│   │           └── MessageStorageImpl.java     # 消息存储实现
│   ├── src/main/resources/
│   │   ├── mapper/                             # MyBatis XML映射
│   │   │   ├── relation/
│   │   │   │   ├── UserToUserMapper.xml
│   │   │   │   ├── UserToDeviceMapper.xml
│   │   │   │   └── UserToSessionMapper.xml
│   │   │   └── message/
│   │   │       ├── MessageMapper.xml
│   │   │       └── MessageStateMapper.xml
│   │   └── schema/                            # 数据库Schema
│   │       ├── init.sql                       # 初始化SQL
│   │       └── tables.sql                     # 建表SQL
│   └── pom.xml
│
└── pom.xml                                      # 父POM文件
```

#### 开发计划（预计 7 周）

##### 第 1 周：plato-storage-api 模块

- [ ] 存储接口定义
    - StorageService 存储服务接口
    - MessageStorage 消息存储接口
    - RelationStorage 关系存储接口
    - TimelineStorage 时间线存储接口
- [ ] 数据模型定义
    - UserRelation 用户关系模型
    - DeviceRelation 设备关系模型
    - SessionRelation 会话关系模型
    - Message 消息模型
    - MessageState 消息状态模型
- [ ] 查询条件定义
    - QueryCriteria 查询条件接口
    - MessageQuery 消息查询条件
    - RelationQuery 关系查询条件
    - TimelineQuery 时间线查询条件

##### 第 2-3 周：plato-storage-core 模块

- [ ] 核心存储实现
    - AbstractStorageService 抽象存储服务
    - DefaultMessageStorage 默认消息存储
    - DefaultRelationStorage 默认关系存储
    - DefaultTimelineStorage 默认时间线存储
- [ ] 存储策略实现
    - StorageStrategy 存储策略接口
    - HotColdStorageStrategy 冷热存储策略
    - ShardingStrategy 分片策略
    - ReplicationStrategy 复制策略
- [ ] 索引管理
    - IndexManager 索引管理器
    - MessageIndex 消息索引
    - TimelineIndex 时间线索引
    - RelationIndex 关系索引

##### 第 4-5 周：数据库实现

- [ ] 关系表设计与实现
    - UserToUserTable 用户关系表
    - UserToDeviceTable 用户设备表
    - UserToSessionTable 用户会话表
    - MessageTable 消息表
    - MessageStateTable 消息状态表
- [ ] MyBatis 映射实现
    - UserRelationMapper 用户关系映射
    - DeviceRelationMapper 设备关系映射
    - SessionRelationMapper 会话关系映射
    - MessageMapper 消息映射
    - MessageStateMapper 状态映射
- [ ] 数据访问层实现
    - UserRelationRepository 用户关系仓储
    - DeviceRelationRepository 设备关系仓储
    - SessionRelationRepository 会话关系仓储
    - MessageRepository 消息仓储
    - MessageStateRepository 状态仓储

##### 第 6-7 周：Timeline 实现与优化

- [ ] 时间线核心实现
    - TimelineService 时间线服务
    - TimelineSegment 时间线分段
    - TimelineIterator 时间线迭代器
- [ ] 消息同步机制
    - MessageSyncService 消息同步服务
    - SyncStrategy 同步策略
    - ConflictResolver 冲突解决器
- [ ] 消息拉取实现
    - MessagePullService 消息拉取服务
    - PullStrategy 拉取策略
    - BatchProcessor 批处理器

#### 开发规范

1. **数据库设计规范**
    - 遵循数据库设计三范式
    - 合理使用索引
    - 分表分库策略明确
    - 字段命名规范统一

2. **存储性能规范**
    - 批量操作优化
    - 索引覆盖优化
    - 读写分离策略
    - 缓存优化策略

#### 监控指标

1. **存储指标**
    - 数据库性能指标
    - 存储空间使用率
    - 查询响应时间
    - 写入吞吐量

2. **业务指标**
    - 消息存储延迟
    - 关系查询性能
    - 时间线拉取效率
    - 数据同步延迟

#### 风险控制

1. **存储风险**
    - 数据一致性保证
    - 高并发写入处理
    - 大数据量查询优化
    - 数据安全保护

2. **性能风险**
    - 慢查询优化
    - 索引效率
    - 存储空间增长
    - 读写性能平衡

#### 测试要求

1. **存储测试**
    - 数据一致性测试
    - 并发压力测试
    - 容量测试
    - 故障恢复测试

2. **性能测试**
    - 读写性能基准测试
    - 大数据量测试
    - 复杂查询性能测试
    - 批量操作性能测试

### protocol 与 network 功能映射

#### plato-protocol 需要完成的 Go 项目功能

根据项目的目标和架构设计，plato-protocol 模块需要实现 Go 项目中以下核心功能：

1. **消息协议定义** (来自 Go 的 protocol 包)
    - 消息结构定义：对应 Go 项目中的消息头、消息体结构
    - 协议版本管理：支持协议版本兼容和演进
    - 消息类型枚举：定义各种消息类型（登录、心跳、上行、下行等）

2. **Protocol Buffers 集成** (来自 Go 的 proto 文件)
    - Proto 文件管理：对应 Go 项目中的 .proto 文件定义
    - 消息序列化/反序列化：基于 Protocol Buffers 的二进制编码
    - 跨语言兼容性：确保与 Go 客户端的兼容通信

3. **编解码器** (来自 Go 的 codec 包)
    - 消息编码：将消息对象转换为二进制格式
    - 消息解码：将二进制数据解析为消息对象
    - 协议解析：处理特定协议格式，如长度前缀帧等

4. **消息工厂** (来自 Go 的 factory 包)
    - 消息创建：统一消息实例的创建逻辑
    - 消息转换：在不同消息格式间进行转换（如 Proto 与领域对象）

5. **自定义二进制协议实现** (来自 Go 的自定义协议)
    - 二进制协议格式：对应 Go 项目中的二进制消息格式
    - 协议兼容性保证：确保与 Go 版本的协议兼容

#### network 功能范围与职责

network 在项目中则负责实现以下功能：

1. **网络传输** (来自 Go 的 network 包)
    - 连接管理：创建、维护和关闭网络连接
    - 数据收发：处理原始网络数据的收发
    - 连接监控：监控网络连接状态

2. **网络事件处理** (来自 Go 的事件处理机制)
    - 连接事件：处理连接建立、断开等事件
    - 读写事件：处理数据读写事件
    - 错误处理：处理网络错误和异常

3. **传输选项** (来自 Go 的 transport 相关代码)
    - 传输配置：设置传输参数如超时、缓冲区大小等
    - 传输策略：实现重传、流控等机制

4. **Socket 连接池管理** (来自 Go 的连接池实现)
    - 连接复用：管理和复用网络连接
    - 连接分配：根据需求分配适当的连接

5. **Netty 集成** (对应 Go 的 epoll 实现)
    - 网络事件循环：实现类似 Go 中 epoll 的事件循环
    - 多路复用：高效处理多连接并发

#### 功能对比与区别

| 功能领域 | plato-protocol 职责 | network 职责 |
|---------|-------------------|-------------|
| **消息定义** | 定义消息结构和类型 | 不涉及 |
| **序列化** | 负责消息序列化/反序列化 | 不涉及 |
| **协议解析** | 解析协议格式和版本 | 不涉及 |
| **连接管理** | 不涉及 | 创建和管理网络连接 |
| **数据传输** | 提供传输层抽象接口 | 实现具体的数据传输 |
| **事件处理** | 消息层面的事件 | 网络层面的事件 |
| **缓冲区管理** | 不涉及 | 管理网络读写缓冲区 |

#### network 在新架构中的实现位置

在新架构中，原 Go 项目中 network 的功能被拆分并重新组织到了不同的模块中，主要分布在以下几个位置：

1. **plato-framework-protocol 的 transport 包**

```
plato-framework/
  ├── plato-framework-protocol/        
      └── src/main/java/com/plato/framework/protocol/
          └── transport/               # 传输层抽象接口
```

这个包包含网络传输的抽象接口，定义了网络通信的基本契约，但不包含具体实现。

2. **plato-access-gateway 模块下的 netty 实现**

```
plato-access/
  ├── plato-access-gateway/            
      └── src/main/java/com/plato/access/gateway/
          └── netty/                   # Netty 网络实现
              ├── handler/             # Netty 处理器
              ├── codec/               # Netty 编解码器
              ├── connection/          # 连接管理
              └── bootstrap/           # 服务器启动配置
```

这里包含了大部分原 network 的核心实现，使用 Netty 替代了 Go 中的 epoll 机制。

3. **plato-framework-common 中的网络相关工具类**

```
plato-framework/
  ├── plato-framework-common/          
      └── src/main/java/com/plato/framework/common/
          └── net/                     # 网络相关工具类
```

这里包含通用的网络工具类，如 IP 地址处理、网络工具方法等。

4. **plato-access-state 中的连接状态管理**

```
plato-access/
  ├── plato-access-state/              
      └── src/main/java/com/plato/access/state/
          └── connection/              # 连接状态管理
```

这个模块负责管理连接的状态信息，是原 network 中连接状态部分的对应实现。

5. **plato-infrastructure 中的网络通信基础组件**

```
plato-infrastructure/
  ├── plato-infrastructure-api/
      └── src/main/java/com/plato/infrastructure/api/
          └── network/                 # 网络通信API
```

这里提供了网络通信的基础设施组件，负责处理底层网络通信细节。

#### 功能映射表

| 原 Go network 功能 | 新架构中的实现位置 |
|--------------------|-------------------|
| 连接建立与管理 | plato-access-gateway/netty/connection |
| 网络事件循环 (epoll) | plato-access-gateway/netty/bootstrap 和 handler |
| 数据收发 | plato-access-gateway/netty/handler |
| 连接池管理 | plato-access-gateway/netty/connection 或 plato-infrastructure-network |
| 网络监控统计 | plato-observability-micrometer |
| 网络错误处理 | plato-access-gateway/netty/handler 和 plato-framework-common/exception |
| Socket 选项配置 | plato-access-gateway/netty/bootstrap |
| 连接状态管理 | plato-access-state/connection |











## Plato-Java 中台化架构重构





### 最新架构



```
plato/
├── docs/                                # 项目文档
│   ├── architecture/                    # 架构设计文档
│   ├── api/                            # API文档
│   └── deployment/                     # 部署文档
│
├── plato-parent/                        # 父项目，管理依赖版本和构建配置
│   └── pom.xml                          # 父POM，定义依赖版本和插件
│
├── plato-bom/                           # Bill of Materials
│   └── pom.xml                          # 依赖版本管理
│
├── plato-framework/                     # 框架核心模块
│   ├── plato-framework-core/            # 框架核心功能
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/
│   │   │       ├── context/            # 应用上下文
│   │   │       ├── exception/          # 异常处理
│   │   │       ├── lifecycle/          # 生命周期管理
│   │   │       └── utils/              # 通用工具类
│   │   └── pom.xml
│   │
│   ├── plato-framework-common/          # 通用工具和基础类
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/common/
│   │   │       ├── constants/          # 常量定义
│   │   │       ├── enums/              # 枚举定义
│   │   │       ├── utils/              # 工具类
│   │   │       └── model/              # 通用数据模型
│   │   └── pom.xml
│   │
│   ├── plato-framework-protocol/        # 协议定义和序列化
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/protocol/
│   │   │       ├── message/            # 消息定义
│   │   │       ├── codec/              # 编解码器
│   │   │       ├── serializer/         # 序列化器
│   │   │       └── transport/          # 传输层抽象
│   │   ├── src/main/resources/
│   │   │   └── proto/                 # Protocol Buffers定义
│   │   └── pom.xml
│   │
│   ├── plato-framework-discovery/       # 服务发现抽象
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/discovery/
│   │   │       ├── api/                # 服务发现API
│   │   │       ├── event/              # 服务事件
│   │   │       └── registry/           # 注册中心抽象
│   │   └── pom.xml
│   │
│   ├── plato-framework-spring/          # Spring集成支持
│   │   ├── src/main/java/
│   │   │   └── com/plato/framework/spring/
│   │   │       ├── config/             # 自动配置
│   │   │       ├── context/            # Spring上下文支持
│   │   │       └── annotation/         # 注解支持
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 框架模块POM
│
├── plato-infrastructure/                # 基础设施模块（独立）
│   ├── plato-infrastructure-api/        # 基础设施API定义
│   │   ├── src/main/java/
│   │   │   └── com/plato/infrastructure/api/
│   │   │       ├── cache/              # 缓存接口
│   │   │       ├── config/             # 配置接口
│   │   │       ├── lock/               # 锁接口
│   │   │       ├── mq/                 # 消息队列接口
│   │   │       └── storage/            # 存储接口
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-cache/      # 缓存中间件集成
│   │   ├── plato-infrastructure-cache-api/      # 缓存API
│   │   ├── plato-infrastructure-cache-redis/    # Redis缓存实现
│   │   ├── plato-infrastructure-cache-caffeine/ # Caffeine本地缓存
│   │   ├── plato-infrastructure-cache-multilevel/ # 多级缓存
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-config/     # 配置中心集成
│   │   ├── plato-infrastructure-config-api/     # 配置API
│   │   ├── plato-infrastructure-config-nacos/   # Nacos配置实现
│   │   ├── plato-infrastructure-config-spring/  # Spring配置实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-lock/       # 分布式锁
│   │   ├── plato-infrastructure-lock-api/       # 锁API
│   │   ├── plato-infrastructure-lock-redis/     # Redis分布式锁
│   │   ├── plato-infrastructure-lock-zookeeper/ # Zookeeper锁
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-mq/         # 消息队列集成
│   │   ├── plato-infrastructure-mq-api/        # 消息队列API
│   │   ├── plato-infrastructure-mq-kafka/      # Kafka实现
│   │   ├── plato-infrastructure-mq-rocketmq/   # RocketMQ实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-db/         # 数据库中间件
│   │   ├── plato-infrastructure-db-api/        # 数据访问API
│   │   ├── plato-infrastructure-db-mybatis/    # MyBatis集成
│   │   ├── plato-infrastructure-db-jpa/        # JPA集成
│   │   ├── plato-infrastructure-db-sharding/   # 分库分表
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-scheduler/  # 调度中间件
│   │   ├── plato-infrastructure-scheduler-api/    # 调度API
│   │   ├── plato-infrastructure-scheduler-quartz/ # Quartz集成
│   │   ├── plato-infrastructure-scheduler-xxljob/ # XXL-Job集成
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-id/         # ID生成器
│   │   ├── plato-infrastructure-id-api/         # ID生成API
│   │   ├── plato-infrastructure-id-leaf/        # Leaf实现
│   │   ├── plato-infrastructure-id-snowflake/   # 雪花算法实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-storage/    # 存储中间件
│   │   ├── plato-infrastructure-storage-api/    # 存储API
│   │   ├── plato-infrastructure-storage-oss/    # 对象存储
│   │   ├── plato-infrastructure-storage-minio/  # MinIO实现
│   │   └── pom.xml
│   │
│   ├── plato-infrastructure-resilience/ # 弹性能力
│   │   ├── plato-infrastructure-resilience-api/       # 弹性API
│   │   ├── plato-infrastructure-resilience-r4j/       # Resilience4j实现
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 基础设施模块POM
│
├── plato-integration/                   # 集成模块
│   ├── plato-integration-spring/        # Spring集成
│   ├── plato-integration-dubbo/         # Dubbo集成
│   ├── plato-integration-sentinel/      # Sentinel集成
│   └── pom.xml
│
├── plato-access/                        # 接入层模块（长连接服务）
│   ├── plato-access-api/                # 接入层API
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/api/
│   │   │       ├── model/              # 接入层数据模型
│   │   │       ├── exception/          # 接入层异常
│   │   │       └── service/            # 接入层服务接口
│   │   └── pom.xml
│   │
│   ├── plato-access-gateway/            # 网关服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/gateway/
│   │   │       ├── config/             # 配置
│   │   │       ├── handler/            # 处理器
│   │   │       ├── netty/              # Netty实现
│   │   │       ├── filter/             # 过滤器
│   │   │       ├── util/               # 工具类
│   │   │       └── service/            # 服务实现
│   │   ├── src/main/resources/
│   │   │   ├── application.yml         # 应用配置
│   │   │   └── bootstrap.yml           # 启动配置
│   │   └── pom.xml
│   │
│   ├── plato-access-ipconf/             # IP配置服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/ipconf/
│   │   │       ├── config/             # 配置
│   │   │       ├── controller/         # 控制器
│   │   │       ├── service/            # 服务实现
│   │   │       └── repository/         # 数据访问
│   │   ├── src/main/resources/
│   │   └── pom.xml
│   │
│   ├── plato-access-state/              # 状态服务
│   │   ├── src/main/java/
│   │   │   └── com/plato/access/state/
│   │   │       ├── config/             # 配置
│   │   │       ├── handler/            # 处理器
│   │   │       ├── processor/          # 消息处理器
│   │   │       ├── service/            # 服务实现
│   │   │       └── repository/         # 数据访问
│   │   ├── src/main/resources/
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 接入层模块POM
│
├── plato-domain/                        # 领域服务模块
│   ├── plato-domain-core/               # 领域核心
│   │   ├── src/main/java/
│   │   │   └── com/plato/domain/core/
│   │   │       ├── annotation/         # 领域注解
│   │   │       ├── exception/          # 领域异常
│   │   │       ├── model/              # 领域模型基类
│   │   │       ├── repository/         # 仓储接口
│   │   │       └── service/            # 服务接口
│   │   └── pom.xml
│   │
│   ├── plato-domain-user/               # 用户领域服务
│   │   ├── plato-domain-user-api/        # 用户领域API
│   │   ├── plato-domain-user-core/       # 用户领域核心
│   │   ├── plato-domain-user-infrastructure/ # 用户领域基础设施
│   │   └── pom.xml
│   │
│   ├── plato-domain-message/            # 消息领域服务
│   │   ├── plato-domain-message-api/
│   │   ├── plato-domain-message-core/
│   │   ├── plato-domain-message-infrastructure/
│   │   └── pom.xml
│   │
│   ├── plato-domain-session/            # 会话领域服务
│   │   ├── plato-domain-session-api/
│   │   ├── plato-domain-session-core/
│   │   ├── plato-domain-session-infrastructure/
│   │   └── pom.xml
│   │
│   ├── plato-domain-relation/           # 关系领域服务
│   │   ├── plato-domain-relation-api/
│   │   ├── plato-domain-relation-core/
│   │   ├── plato-domain-relation-infrastructure/
│   │   └── pom.xml
│   │
│   └── pom.xml                          # 领域服务模块POM
│
├── plato-adapter/                       # 业务适配层
│   ├── plato-adapter-api/               # 适配层API
│   ├── plato-adapter-im/                # IM业务适配
│   ├── plato-adapter-push/              # 推送业务适配
│   └── pom.xml
│
├── plato-event-center/                  # 事件中心
│   ├── plato-event-center-api/          # 事件中心API
│   ├── plato-event-center-core/         # 事件中心核心实现
│   ├── plato-event-center-processor/    # 事件处理器
│   └── pom.xml
│
├── plato-storage/                       # 存储服务模块
│   ├── plato-storage-api/               # 存储API
│   ├── plato-storage-core/              # 存储核心实现
│   ├── plato-storage-oss/               # 对象存储实现
│   └── pom.xml
│
├── plato-client-sdk/                    # 客户端SDK
│   ├── plato-client-sdk-core/           # SDK核心
│   ├── plato-client-sdk-java/           # Java客户端
│   ├── plato-client-sdk-android/        # Android客户端
│   └── pom.xml
│
├── plato-api/                           # API网关
│   ├── plato-api-core/                  # API核心
│   ├── plato-api-rest/                  # REST API实现
│   ├── plato-api-grpc/                  # gRPC API实现
│   └── pom.xml
│
├── plato-bizflow/                       # 业务流程引擎（自研核心模块）
│   ├── plato-bizflow-api/               # 流程引擎API
│   ├── plato-bizflow-core/              # 流程引擎核心
│   │   ├── src/main/java/
│   │   │   └── com/plato/bizflow/core/
│   │   │       ├── engine/             # 引擎实现
│   │   │       ├── model/              # 流程模型
│   │   │       ├── execution/          # 执行器
│   │   │       ├── parser/             # 流程解析器
│   │   │       └── storage/            # 流程存储
│   │   └── pom.xml
│   │
│   ├── plato-bizflow-dsl/               # 流程DSL
│   ├── plato-bizflow-spring/            # Spring集成
│   └── pom.xml
│
├── plato-observability/                 # 可观察性模块
│   ├── plato-observability-api/         # 可观察性API
│   ├── plato-observability-actuator/    # Spring Boot Actuator集成
│   ├── plato-observability-micrometer/  # Micrometer指标收集
│   │   ├── src/main/java/
│   │   │   └── com/plato/observability/micrometer/
│   │   │       ├── config/              # 配置
│   │   │       ├── metrics/             # 指标定义
│   │   │       ├── binder/              # 指标绑定器
│   │   │       └── exporter/            # 指标导出器
│   │   └── pom.xml
│   │
│   ├── plato-observability-tracing/     # 分布式追踪
│   ├── plato-observability-logging/     # 日志增强
│   ├── plato-observability-dashboard/   # 监控仪表盘
│   └── pom.xml
│
├── plato-boot/                          # 启动模块
│   ├── plato-boot-core/                 # 核心启动功能
│   │   ├── src/main/java/
│   │   │   └── com/plato/boot/core/
│   │   │       ├── launcher/            # 启动器
│   │   │       ├── config/              # 配置加载
│   │   │       ├── lifecycle/           # 生命周期管理
│   │   │       └── listener/            # 事件监听器
│   │   └── pom.xml
│   │
│   ├── plato-boot-config/               # 配置加载
│   ├── plato-boot-cli/                  # 命令行工具
│   └── pom.xml
│
├── plato-web/                          # Web应用模块
│   ├── plato-web-admin/                # 管理后台
│   ├── plato-web-portal/               # 用户门户
│   └── pom.xml
│
├── plato-example/                      # 示例模块
│   ├── plato-example-basic/            # 基础使用示例
│   ├── plato-example-advanced/         # 高级特性示例
│   └── pom.xml
│
├── plato-ops/                          # 运维模块
│   ├── plato-ops-deploy/               # 部署脚本
│   ├── plato-ops-monitor/              # 监控配置
│   ├── plato-ops-alert/                # 告警配置
│   └── docker/                         # Docker配置
│       ├── Dockerfile                  # 应用Dockerfile
│       └── docker-compose.yml          # 本地开发环境
│
├── .gitignore                          # Git忽略配置
├── mvnw                                # Maven包装器
├── mvnw.cmd                            # Maven包装器(Windows)
├── pom.xml                             # 根POM
└── README.md                           # 项目说明
```





基于对 Go 项目功能分析，我们将分阶段实现 Java 版 Plato 系统，首先专注于核心功能的复现，然后再扩展其他模块。

### 第一阶段：核心基础设施实现

这个阶段专注于构建基础框架，确保后续各模块能正常工作。

#### 1. 基础框架模块 (plato-framework)

**1.1 plato-common**

- 异常体系设计
- 通用工具类实现(日志、时间、字符串处理等)
- 通用模型定义

**1.2 plato-protocol**

- 消息协议定义(参考 Go 的自定义二进制协议)
- 编解码器实现
- 消息头和消息体结构

**1.3 plato-discovery**

- 服务注册接口
- 服务发现接口
- Nacos/ZooKeeper 实现

**1.4 plato-infrastructure**

- 配置管理(对应 Go 的 common/config)
- 缓存实现(对应 Go 的 common/cache)
- 客户端调用(对应 Go 的 common/prpc)

#### 2. 启动与配置模块 (plato-boot)

- 配置加载(对应 Go 的 config.Init)
- 启动入口(对应 Go 的 cmd 模块)
- 服务生命周期管理

### 第二阶段：核心接入层实现

这个阶段实现的是系统的接入层，负责管理客户端连接。

#### 3. 接入层模块 (plato-access)

**3.1 plato-gateway**

- 长连接服务器(使用 Netty 实现，对应 Go 的 gateway)
- 连接管理(对应 Go 的 connection.go 和 epoll_linux.go)
- 消息路由(对应 Go 的 router)
- 工作池实现(对应 Go 的 workpool.go)

**3.2 plato-state**

- 状态服务实现(对应 Go 的 state)
- 会话管理(对应 Go 的 state.go)
- 心跳机制(对应 Go 的 timer.go)
- 状态缓存(对应 Go 的 cache.go)

**3.3 plato-ipconf**

- IP 配置服务(对应 Go 的 ipconf)
- 负载均衡算法(对应 Go 的 domain/ipconf)
- 服务健康检查
- 地理位置服务

### 第三阶段：客户端 SDK 实现

这个阶段实现客户端 SDK，便于业务系统接入 Plato 系统。

#### 4. 客户端 SDK 模块 (plato-client-sdk)

**4.1 plato-client-core**

- 连接管理
- 认证鉴权
- 消息发送与接收

**4.2 plato-client-message**

- 消息类型定义
- 消息处理
- 消息 ACK 机制

### 第四阶段：性能监控实现

这个阶段实现性能监控，确保系统运行状态可观测。

#### 5. 性能监控模块 (plato-perf)

- 性能指标收集
- 性能数据处理
- 监控报告生成

### 第五阶段：领域层基础实现

这个阶段实现最基本的领域服务。

#### 6. 领域服务模块 (plato-domain)

**6.1 plato-domain-user**

- 用户领域模型
- 用户服务接口
- 用户认证逻辑

### 实现优先级和顺序

为了确保系统能尽快运行起来，建议按照以下顺序实现：

1. **最高优先级** (系统运行的基础)
    - plato-protocol (网络协议)
    - plato-discovery (服务发现)
    - plato-boot (启动配置)
    - plato-gateway (网关服务)

2. **高优先级** (系统核心功能)
    - plato-state (状态服务)
    - plato-ipconf (负载均衡)
    - plato-client-core (核心 SDK)

3. **中优先级** (功能完善)
    - plato-infrastructure (基础设施)
    - plato-client-message (消息 SDK)
    - plato-domain-user (用户服务)

4. **低优先级** (监控与优化)
    - plato-perf (性能监控)

### 技术实现对照表

| Go 项目组件      | Java 实现技术选型         |
| --------------- | ------------------------ |
| epoll/iocp      | Netty EventLoop          |
| Protobuf        | Protobuf-Java            |
| etcd            | Nacos/ZooKeeper          |
| gRPC            | gRPC-Java/Spring Cloud   |
| Goroutine 工作池 | 线程池/虚拟线程(Java 21) |
| 时间轮算法      | HashedWheelTimer         |
| 命令行工具      | Spring Shell/Picocli     |
| 缓存实现        | Redis + Spring Cache     |
| 日志系统        | SLF4J + Logback          |

### 核心模块开发计划

### 第一阶段：基础框架模块 (plato-framework)

#### 1. plato-common 模块 (1 周)

- [ ] 异常体系设计
    - PlatoException 基类
    - BusinessException 业务异常
    - SystemException 系统异常
    - NetworkException 网络异常
- [ ] 通用工具类实现
    - HashUtils 哈希工具
    - StringUtils 字符串处理
    - DateUtils 日期处理
    - CollectionUtils 集合处理
- [ ] 通用模型定义
    - Result 统一返回结果
    - Page 分页模型
    - BaseEntity 基础实体

#### 2. plato-protocol 模块 (1 周)

- [ ] 消息协议定义
    - 消息头结构
    - 消息体结构
    - 消息类型枚举
- [ ] 编解码器实现
    - MessageEncoder 消息编码器
    - MessageDecoder 消息解码器
    - MessageCodec 消息编解码器
- [ ] 序列化实现
    - Protobuf 序列化器
    - JSON 序列化器
    - 自定义二进制序列化器

#### 3. plato-discovery 模块 (1 周)

- [ ] 服务发现接口
    - ServiceRegistry 服务注册
    - ServiceDiscovery 服务发现
    - ServiceInstance 服务实例
- [ ] Nacos 实现
    - NacosServiceRegistry
    - NacosServiceDiscovery
    - NacosServiceInstance
- [ ] 服务健康检查
    - HealthCheck 接口
    - DefaultHealthCheck 实现

#### 4. plato-infrastructure 模块 (2 周)

- [ ] 配置管理
    - ConfigService 配置服务
    - LocalConfigService 本地配置
    - NacosConfigService 远程配置
- [ ] 缓存实现
    - CacheService 缓存服务
    - RedisCacheService 实现
    - LocalCacheService 实现
- [ ] 客户端调用
    - RpcClient 接口
    - GrpcClient 实现
    - RestClient 实现

### 第二阶段：接入层模块 (plato-access)

#### 1. plato-gateway 模块 (2 周)

- [ ] Netty 服务器实现
    - ServerBootstrap 配置
    - ChannelHandler 实现
    - MessageHandler 实现
- [ ] 连接管理
    - ConnectionManager 接口
    - DefaultConnectionManager 实现
    - ConnectionPool 连接池
- [ ] 消息路由
    - Router 接口
    - DefaultRouter 实现
    - RouteRule 路由规则

#### 2. plato-state 模块 (2 周)

- [ ] 状态服务实现
    - StateService 接口
    - DefaultStateService 实现
    - StateRepository 存储
- [ ] 会话管理
    - SessionManager 接口
    - DefaultSessionManager 实现
    - SessionRepository 存储
- [ ] 心跳机制
    - HeartbeatService 接口
    - DefaultHeartbeatService 实现
    - HeartbeatMonitor 监控

#### 3. plato-ipconf 模块 (1 周)

- [ ] IP 配置服务
    - IpConfService 接口
    - DefaultIpConfService 实现
    - IpConfRepository 存储
- [ ] 负载均衡
    - LoadBalancer 接口
    - RoundRobinLoadBalancer 实现
    - WeightedLoadBalancer 实现
- [ ] 健康检查
    - HealthChecker 接口
    - DefaultHealthChecker 实现
    - HealthCheckMonitor 监控

### 开发规范

1. **代码规范**
    - 遵循阿里巴巴 Java 开发手册
    - 使用 CheckStyle 进行代码风格检查
    - 使用 SpotBugs 进行代码质量检查

2. **测试规范**
    - 每个模块必须包含单元测试
    - 核心功能必须包含集成测试
    - 测试覆盖率要求达到 80%以上

3. **文档规范**
    - 每个模块必须包含 README.md
    - 核心接口必须有 JavaDoc 注释
    - 复杂逻辑必须有详细注释

4. **版本控制**
    - 使用 Git Flow 工作流
    - 每个功能使用 feature 分支
    - 提交信息必须清晰描述变更内容

### 开发流程

1. **需求分析**
    - 分析 Go 项目对应模块的功能
    - 设计 Java 版本的实现方案
    - 编写详细设计文档

2. **开发实现**
    - 创建模块基础结构
    - 实现核心功能
    - 编写单元测试
    - 进行代码审查

3. **集成测试**
    - 模块间集成测试
    - 性能测试
    - 压力测试

4. **文档完善**
    - 更新 API 文档
    - 编写使用示例
    - 更新项目文档

### 时间安排

- 第一阶段：5 周
    - plato-common: 1 周
    - plato-protocol: 1 周
    - plato-discovery: 1 周
    - plato-infrastructure: 2 周

- 第二阶段：5 周
    - plato-gateway: 2 周
    - plato-state: 2 周
    - plato-ipconf: 1 周

总计：10 周完成核心模块开发

### 风险控制

1. **技术风险**
    - Netty 性能优化
    - 分布式一致性
    - 高并发处理

2. **进度风险**
    - 模块间依赖关系
    - 技术难点攻关
    - 测试覆盖率要求

3. **质量风险**
    - 代码质量保证
    - 性能指标达标
    - 系统稳定性

### 监控指标

1. **开发进度**
    - 模块完成度
    - 测试覆盖率
    - 代码审查通过率

2. **代码质量**
    - 代码规范符合度
    - 代码重复率
    - 圈复杂度

3. **性能指标**
    - 响应时间
    - 并发处理能力
    - 资源使用率

### 第三阶段：存储模块实现 (plato-storage)

#### 1. plato-storage-api 模块 (1 周)

- [ ] 存储接口定义
    - StorageService 存储服务接口
    - MessageStorage 消息存储接口
    - RelationStorage 关系存储接口
    - TimelineStorage 时间线存储接口
- [ ] 数据模型定义
    - UserRelation 用户关系模型
    - DeviceRelation 设备关系模型
    - SessionRelation 会话关系模型
    - Message 消息模型
    - MessageState 消息状态模型
- [ ] 查询条件定义
    - QueryCriteria 查询条件接口
    - MessageQuery 消息查询条件
    - RelationQuery 关系查询条件
    - TimelineQuery 时间线查询条件

#### 2. plato-storage-core 模块 (2 周)

- [ ] 核心存储实现
    - AbstractStorageService 抽象存储服务
    - DefaultMessageStorage 默认消息存储
    - DefaultRelationStorage 默认关系存储
    - DefaultTimelineStorage 默认时间线存储
- [ ] 存储策略实现
    - StorageStrategy 存储策略接口
    - HotColdStorageStrategy 冷热存储策略
    - ShardingStrategy 分片策略
    - ReplicationStrategy 复制策略
- [ ] 索引管理
    - IndexManager 索引管理器
    - MessageIndex 消息索引
    - TimelineIndex 时间线索引
    - RelationIndex 关系索引

#### 3. plato-storage-rdb 模块 (2 周)

- [ ] 关系表设计与实现
    - UserToUserTable 用户关系表
    - UserToDeviceTable 用户设备表
    - UserToSessionTable 用户会话表
    - MessageTable 消息表
    - MessageStateTable 消息状态表
- [ ] MyBatis 映射实现
    - UserRelationMapper 用户关系映射
    - DeviceRelationMapper 设备关系映射
    - SessionRelationMapper 会话关系映射
    - MessageMapper 消息映射
    - MessageStateMapper 状态映射
- [ ] 数据访问层实现
    - UserRelationRepository 用户关系仓储
    - DeviceRelationRepository 设备关系仓储
    - SessionRelationRepository 会话关系仓储
    - MessageRepository 消息仓储
    - MessageStateRepository 状态仓储

#### 4. plato-storage-timeline 模块 (2 周)

- [ ] 时间线核心实现
    - TimelineService 时间线服务
    - TimelineSegment 时间线分段
    - TimelineIterator 时间线迭代器
- [ ] 消息同步机制
    - MessageSyncService 消息同步服务
    - SyncStrategy 同步策略
    - ConflictResolver 冲突解决器
- [ ] 消息拉取实现
    - MessagePullService 消息拉取服务
    - PullStrategy 拉取策略
    - BatchProcessor 批处理器

### 开发规范补充

1. **数据库设计规范**
    - 遵循数据库设计三范式
    - 合理使用索引
    - 分表分库策略明确
    - 字段命名规范统一

2. **存储性能规范**
    - 批量操作优化
    - 索引覆盖优化
    - 读写分离策略
    - 缓存优化策略

### 时间安排补充

- 第三阶段：7 周
    - plato-storage-api: 1 周
    - plato-storage-core: 2 周
    - plato-storage-rdb: 2 周
    - plato-storage-timeline: 2 周

### 风险控制补充

1. **存储风险**
    - 数据一致性保证
    - 高并发写入处理
    - 大数据量查询优化
    - 数据安全保护

2. **性能风险**
    - 慢查询优化
    - 索引效率
    - 存储空间增长
    - 读写性能平衡

### 监控指标补充

1. **存储指标**
    - 数据库性能指标
    - 存储空间使用率
    - 查询响应时间
    - 写入吞吐量

2. **业务指标**
    - 消息存储延迟
    - 关系查询性能
    - 时间线拉取效率
    - 数据同步延迟

### 测试要求补充

1. **存储测试**
    - 数据一致性测试
    - 并发压力测试
    - 容量测试
    - 故障恢复测试

2. **性能测试**
    - 读写性能基准测试
    - 大数据量测试
    - 复杂查询性能测试
    - 批量操作性能测试







### plato-storage 模块详细目录结构

```
plato-storage/
├── plato-storage-api/                           # 存储API模块
│   ├── src/main/java/
│   │   └── com/plato/storage/api/
│   │       ├── constant/                        # 常量定义
│   │       │   ├── StorageConstants.java        # 存储相关常量
│   │       │   └── StorageErrorCode.java        # 错误码定义
│   │       ├── enums/                           # 枚举定义
│   │       │   ├── MessageType.java            # 消息类型
│   │       │   └── MessageStatus.java          # 消息状态
│   │       ├── model/                           # 数据模型
│   │       │   ├── relation/                    # 关系相关模型
│   │       │   │   ├── UserToUser.java         # 用户-用户关系
│   │       │   │   ├── UserToDevice.java       # 用户-设备关系
│   │       │   │   └── UserToSession.java      # 用户-会话关系
│   │       │   └── message/                     # 消息相关模型
│   │       │       ├── Message.java            # 消息实体
│   │       │       └── MessageState.java       # 消息状态
│   │       └── service/                         # 服务接口
│   │           ├── RelationStorage.java        # 关系存储接口
│   │           └── MessageStorage.java         # 消息存储接口
│   └── pom.xml
│
├── plato-storage-core/                          # 存储核心实现
│   ├── src/main/java/
│   │   └── com/plato/storage/core/
│   │       ├── config/                          # 配置类
│   │       │   ├── StorageAutoConfiguration.java # 自动配置
│   │       │   ├── StorageProperties.java      # 配置属性
│   │       │   └── MybatisConfiguration.java   # MyBatis配置
│   │       ├── repository/                      # 数据访问层
│   │       │   ├── relation/                    # 关系仓储
│   │       │   │   ├── UserToUserRepository.java
│   │       │   │   ├── UserToDeviceRepository.java
│   │       │   │   └── UserToSessionRepository.java
│   │       │   └── message/                     # 消息仓储
│   │       │       ├── MessageRepository.java
│   │       │       └── MessageStateRepository.java
│   │       ├── entity/                          # 数据库实体
│   │       │   ├── relation/                    # 关系实体
│   │       │   │   ├── UserToUserDO.java
│   │       │   │   ├── UserToDeviceDO.java
│   │       │   │   └── UserToSessionDO.java
│   │       │   └── message/                     # 消息实体
│   │       │       ├── MessageDO.java
│   │       │       └── MessageStateDO.java
│   │       ├── mapper/                          # MyBatis映射
│   │       │   ├── relation/                    # 关系Mapper
│   │       │   │   ├── UserToUserMapper.java
│   │       │   │   ├── UserToDeviceMapper.java
│   │       │   │   └── UserToSessionMapper.java
│   │       │   └── message/                     # 消息Mapper
│   │       │       ├── MessageMapper.java
│   │       │       └── MessageStateMapper.java
│   │       └── service/                         # 服务实现
│   │           ├── RelationStorageImpl.java    # 关系存储实现
│   │           └── MessageStorageImpl.java     # 消息存储实现
│   ├── src/main/resources/
│   │   ├── mapper/                             # MyBatis XML映射
│   │   │   ├── relation/
│   │   │   │   ├── UserToUserMapper.xml
│   │   │   │   ├── UserToDeviceMapper.xml
│   │   │   │   └── UserToSessionMapper.xml
│   │   │   └── message/
│   │   │       ├── MessageMapper.xml
│   │   │       └── MessageStateMapper.xml
│   │   └── schema/                            # 数据库Schema
│   │       ├── init.sql                       # 初始化SQL
│   │       └── tables.sql                     # 建表SQL
│   └── pom.xml
│
└── pom.xml                                      # 父POM文件
```

### 模块说明

1. **plato-storage-api**
    - 定义存储相关的接口和模型
    - 包含关系存储和消息存储的核心接口
    - 定义数据模型和查询接口

2. **plato-storage-core**
    - 实现核心存储逻辑
    - 提供基础的数据访问层实现
    - 管理数据库实体和映射
    - 包含 MyBatis 相关实现和配置
    - 包含数据库表结构和初始化脚本

3. **plato-storage-mysql**
    - MySQL 具体实现
    - MyBatis 映射和 SQL 定义
    - 数据库表结构和初始化脚本

### 关键文件说明

1. **数据模型**
    - `UserToUser.java`: 用户-用户关系模型
    - `UserToDevice.java`: 用户-设备关系模型
    - `UserToSession.java`: 用户-会话关系模型
    - `Message.java`: 消息实体模型
    - `MessageState.java`: 消息状态模型

2. **存储接口**
    - `RelationStorage.java`: 关系存储接口
    - `MessageStorage.java`: 消息存储接口

3. **数据库表**
    - `tables.sql`: 包含所有表的建表语句
        - user_to_user 表
        - user_to_device 表
        - user_to_session 表
        - msg 表
        - msg_state 表

4. **数据访问层**
    - `UserToUserRepository.java`: 用户关系仓储
    - `MessageRepository.java`: 消息仓储
    - `MessageStateRepository.java`: 消息状态仓储

### 2023 年 5 月 1 日 - plato-storage 模块开发计划

#### 目标

实现 plato-storage 模块，提供统一的存储服务，支持消息和关系数据的存储、查询和管理。

#### 模块详细目录结构

```
plato-storage/
├── plato-storage-api/                           # 存储API模块
│   ├── src/main/java/
│   │   └── com/plato/storage/api/
│   │       ├── constant/                        # 常量定义
│   │       │   ├── StorageConstants.java        # 存储相关常量
│   │       │   └── StorageErrorCode.java        # 错误码定义
│   │       ├── enums/                           # 枚举定义
│   │       │   ├── MessageType.java            # 消息类型
│   │       │   └── MessageStatus.java          # 消息状态
│   │       ├── model/                           # 数据模型
│   │       │   ├── relation/                    # 关系相关模型
│   │       │   │   ├── UserToUser.java         # 用户-用户关系
│   │       │   │   ├── UserToDevice.java       # 用户-设备关系
│   │       │   │   └── UserToSession.java      # 用户-会话关系
│   │       │   └── message/                     # 消息相关模型
│   │       │       ├── Message.java            # 消息实体
│   │       │       └── MessageState.java       # 消息状态
│   │       └── service/                         # 服务接口
│   │           ├── RelationStorage.java        # 关系存储接口
│   │           └── MessageStorage.java         # 消息存储接口
│   └── pom.xml
│
├── plato-storage-core/                          # 存储核心实现
│   ├── src/main/java/
│   │   └── com/plato/storage/core/
│   │       ├── config/                          # 配置类
│   │       │   ├── StorageAutoConfiguration.java # 自动配置
│   │       │   ├── StorageProperties.java      # 配置属性
│   │       │   └── MybatisConfiguration.java   # MyBatis配置
│   │       ├── repository/                      # 数据访问层
│   │       │   ├── relation/                    # 关系仓储
│   │       │   │   ├── UserToUserRepository.java
│   │       │   │   ├── UserToDeviceRepository.java
│   │       │   │   └── UserToSessionRepository.java
│   │       │   └── message/                     # 消息仓储
│   │       │       ├── MessageRepository.java
│   │       │       └── MessageStateRepository.java
│   │       ├── entity/                          # 数据库实体
│   │       │   ├── relation/                    # 关系实体
│   │       │   │   ├── UserToUserDO.java
│   │       │   │   ├── UserToDeviceDO.java
│   │       │   │   └── UserToSessionDO.java
│   │       │   └── message/                     # 消息实体
│   │       │       ├── MessageDO.java
│   │       │       └── MessageStateDO.java
│   │       ├── mapper/                          # MyBatis映射
│   │       │   ├── relation/                    # 关系Mapper
│   │       │   │   ├── UserToUserMapper.java
│   │       │   │   ├── UserToDeviceMapper.java
│   │       │   │   └── UserToSessionMapper.java
│   │       │   └── message/                     # 消息Mapper
│   │       │       ├── MessageMapper.java
│   │       │       └── MessageStateMapper.java
│   │       └── service/                         # 服务实现
│   │           ├── RelationStorageImpl.java    # 关系存储实现
│   │           └── MessageStorageImpl.java     # 消息存储实现
│   ├── src/main/resources/
│   │   ├── mapper/                             # MyBatis XML映射
│   │   │   ├── relation/
│   │   │   │   ├── UserToUserMapper.xml
│   │   │   │   ├── UserToDeviceMapper.xml
│   │   │   │   └── UserToSessionMapper.xml
│   │   │   └── message/
│   │   │       ├── MessageMapper.xml
│   │   │       └── MessageStateMapper.xml
│   │   └── schema/                            # 数据库Schema
│   │       ├── init.sql                       # 初始化SQL
│   │       └── tables.sql                     # 建表SQL
│   └── pom.xml
│
└── pom.xml                                      # 父POM文件
```

#### 开发计划（预计 7 周）

##### 第 1 周：plato-storage-api 模块

- [ ] 存储接口定义
    - StorageService 存储服务接口
    - MessageStorage 消息存储接口
    - RelationStorage 关系存储接口
    - TimelineStorage 时间线存储接口
- [ ] 数据模型定义
    - UserRelation 用户关系模型
    - DeviceRelation 设备关系模型
    - SessionRelation 会话关系模型
    - Message 消息模型
    - MessageState 消息状态模型
- [ ] 查询条件定义
    - QueryCriteria 查询条件接口
    - MessageQuery 消息查询条件
    - RelationQuery 关系查询条件
    - TimelineQuery 时间线查询条件

##### 第 2-3 周：plato-storage-core 模块

- [ ] 核心存储实现
    - AbstractStorageService 抽象存储服务
    - DefaultMessageStorage 默认消息存储
    - DefaultRelationStorage 默认关系存储
    - DefaultTimelineStorage 默认时间线存储
- [ ] 存储策略实现
    - StorageStrategy 存储策略接口
    - HotColdStorageStrategy 冷热存储策略
    - ShardingStrategy 分片策略
    - ReplicationStrategy 复制策略
- [ ] 索引管理
    - IndexManager 索引管理器
    - MessageIndex 消息索引
    - TimelineIndex 时间线索引
    - RelationIndex 关系索引

##### 第 4-5 周：数据库实现

- [ ] 关系表设计与实现
    - UserToUserTable 用户关系表
    - UserToDeviceTable 用户设备表
    - UserToSessionTable 用户会话表
    - MessageTable 消息表
    - MessageStateTable 消息状态表
- [ ] MyBatis 映射实现
    - UserRelationMapper 用户关系映射
    - DeviceRelationMapper 设备关系映射
    - SessionRelationMapper 会话关系映射
    - MessageMapper 消息映射
    - MessageStateMapper 状态映射
- [ ] 数据访问层实现
    - UserRelationRepository 用户关系仓储
    - DeviceRelationRepository 设备关系仓储
    - SessionRelationRepository 会话关系仓储
    - MessageRepository 消息仓储
    - MessageStateRepository 状态仓储

##### 第 6-7 周：Timeline 实现与优化

- [ ] 时间线核心实现
    - TimelineService 时间线服务
    - TimelineSegment 时间线分段
    - TimelineIterator 时间线迭代器
- [ ] 消息同步机制
    - MessageSyncService 消息同步服务
    - SyncStrategy 同步策略
    - ConflictResolver 冲突解决器
- [ ] 消息拉取实现
    - MessagePullService 消息拉取服务
    - PullStrategy 拉取策略
    - BatchProcessor 批处理器

#### 开发规范

1. **数据库设计规范**
    - 遵循数据库设计三范式
    - 合理使用索引
    - 分表分库策略明确
    - 字段命名规范统一

2. **存储性能规范**
    - 批量操作优化
    - 索引覆盖优化
    - 读写分离策略
    - 缓存优化策略

#### 监控指标

1. **存储指标**
    - 数据库性能指标
    - 存储空间使用率
    - 查询响应时间
    - 写入吞吐量

2. **业务指标**
    - 消息存储延迟
    - 关系查询性能
    - 时间线拉取效率
    - 数据同步延迟

#### 风险控制

1. **存储风险**
    - 数据一致性保证
    - 高并发写入处理
    - 大数据量查询优化
    - 数据安全保护

2. **性能风险**
    - 慢查询优化
    - 索引效率
    - 存储空间增长
    - 读写性能平衡

#### 测试要求

1. **存储测试**
    - 数据一致性测试
    - 并发压力测试
    - 容量测试
    - 故障恢复测试

2. **性能测试**
    - 读写性能基准测试
    - 大数据量测试
    - 复杂查询性能测试
    - 批量操作性能测试
